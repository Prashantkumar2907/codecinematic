node_modules/
.next/
.env
.env*.local
output/
content/videos/
content/news/
*.log
.venv/
*.tsbuildinfo
.DS_Store
content/factory/
# Local state, EXCEPT four files that are force-tracked (`git add -f`) because
# they are not regenerable for free: channels/history/quota/subjects, and
# briefs.json — 373 one-time Gemini calls (row 13.2). A negation rule cannot
# re-include them, since git will not descend into an excluded directory.
content/
graphify-out/
playwright-report/
audit/
.gemini/
*.ts.bak
# Animation-QA capture artifacts (regenerate with: npm run filmstrip)
qa/**/*.png
# Superseded by the above: filmstrip writes to qa/<kind>/, not the repo root
screenshot_*.png

# lexicon-check --voice renders listening pairs; regenerable like qa/<kind>/
qa/lexicon/
