# Animation QA ledger

Source of truth for polish progress across all 111 registered painters — **not** the
conversation, which gets summarised and lost. Update a row in the same commit that polishes its kind.

- Rubric and ship gate: `ANIMATION-QA-PROMPT.md` Part C. **Ship gate: every section >= 4.**
- Score columns are 1-5: cont = containment & safe area, typo = typography, motion = motion quality,
  clean = cleanliness, palette = palette & consistency.
- `status`: todo | in-progress | passed | blocked. `blocked` must carry the specific reason in summary.
- Typecheck baseline: **99** pre-existing errors (`qa/ledger.json` → `typecheckBaseline`).
  A polish commit may never raise it, and the file it touched must have zero errors.
- Variant-seeded painters (`variantOf(scene.id, n)`) only show one style per scene id. Reach the
  others with `npm run filmstrip -- --scene=<sceneId>` (output goes to `qa/<kind>/<sceneId>/`).
- Motion scoring needs `--entrance` (samples the first 500ms at ~33ms/cell, one real frame at 30fps).
  A plain `p=0..1` strip has 533ms-2.7s between cells and cannot show a 380ms `enterT()` entrance.
- Capture: `npm run filmstrip -- --kind=<kind>` writes `qa/<kind>/{short,long}-strip.png`,
  `-p50.png`, `-p90.png` and `console.log`. Those artifacts are gitignored; the ledger is not.

> **Systemic, see `qa/ledger.json` -> `systemic`:** the 3D layer of 29 painters was frozen at frame 0
> (`render3D` cached a closure over the first frame`s env). Fixed centrally on 2026-07-27, but only
> bigtext and bullets have been looked at since. The other 27 animate for the first time and MUST be
> re-reviewed when the wave reaches them.

| kind | wave | group | status | rounds | cont | typo | motion | clean | palette | summary | commit | date |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| bigtext | 1 | Text/general | passed | 5 | 4 | 4 | 4 | 4 | 4 | All 5 variants scored 4/5. Fixed: entrance clipping (1.8x stamp overflowed canvas), Shorts safe-area overrun, 350ms hard colour pop, hardcoded glitch hex, Math.random nondeterminism, icon flying through headline, v0 accent bar at full opacity at t=0, v2 letters cascading through the icon, 3D grid hardcoded hex. Harness gained --scene= and --entrance to get here. | (uncommitted) | 2026-07-27 |
| bullets | 19 | Text/general | passed | 2 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js panel removed (rows fill via roundRect + applyElevation, no fallback existed before — `if (!cam) return` drew nothing when WebGL was unavailable). departT wraps the whole scene. Dropped a dead `variantOf` import (never called). **Occupancy defect found**: short scored 1/5 (dead 24%, still 66%) once a long list finished entering and just held — the active row's shadowBlur border pulse covered too little of the frame to register. Replaced with the whole row's fill breathing via lerpColor+idle, matching table's fix. 5/5 both aspects; edge-check 0.0%; safe-check clear. | (uncommitted) | 2026-08-08 |
| quote | 19 | Text/general | passed | 2 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js glass block + camera bob removed, replaced with a plain 2D translate (same visual float, no projection round-trip needed). **Variant collapsed by ASPECT, not scene-id**: `leftBar = !vertical`, giving long a deliberate editorial left-rail composition and short a deliberate centered one; the third 'fullBleed' giant-watermark treatment dropped (competed with the text rather than supporting it) — same move as stat/mythfact, satisfying one-look-per-kind and s10 together. **Tuning found live**: bob amplitude 0.1u scored 2/5 (still ~42%, too subtle to register at the motion-check grid); 0.4u scored 4/5 (too much — inflated the median so the real discrete beats, the rule-grow and author-fade, no longer spiked above it and the events count dropped to 1); 0.2u lands at 5/5 both aspects with 6 events. departT wraps the scene. edge-check 0.0%; safe-check clear. Verified visually via filmstrip. | (uncommitted) | 2026-08-08 |
| stat | 19 | Text/general | passed | 2 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: isoCamera/three.js glass panel removed, replaced with a 2D gradient card (roundRect + applyElevation). **Variant collapsed by ASPECT, not scene-id**: the old `variantOf(scene.id,2)` left/centered choice was random per scene; now `leftLayout = !vertical`, giving long a deliberate left-number/context-beside composition and short a deliberate centered/context-below one — satisfies both 'one look per kind' and the new per-aspect composition axis (s10) at once. **Two genuine defects found**: (1) containment 5.0% left/right at 9:16 — the panel width derived from the value's own text width could exceed `contentW` on a narrow aspect; clamped to the frame. (2) occupancy 2/5 short (still 54%) — a several-px bob was below the motion-check grid's resolution and read as fully still; amplitude raised to `unit*0.4` plus a subtle breathing scale, both large enough on a big panel to register. 5/5 both aspects; edge-check 0.0%; safe-check clear. Verified visually via filmstrip both aspects. | (uncommitted) | 2026-08-08 |
| steps | 19 | Text/general | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: no three.js, no variant to collapse — pure occupancy fix. **Was the reference example for the class of defect this phase exists to fix**: motion-check scored 1/5 both aspects, median diff 0.009 (essentially frozen) — numbers pop in per beat then hold completely still, still 64-68%. Added a breathing highlight band across the active row's text (idle-tied, low-alpha accent wash) plus tied the number's glow to idle() instead of a static value. departT wraps the scene. 5/5 both aspects, still 5-8%; edge-check 0.0%; safe-check clear. | (uncommitted) | 2026-08-08 |
| vocab | 1 | Text/general | passed | 3 | 4 | 4 | 4 | 4 | 4 | 2/3/4/1/2 -> 4/4/4/4/4. The 3D slabs and the example text were unrelated layers: an off-axis camera plus a per-row Z stagger meant a slab projected as a diagonal parallelogram while the text was centred on the projected world origin, so they overlapped. Front-on camera, coplanar rows, card size from frustumHalfExtent, text box derived from the projected front face. Also killed a 26%-of-frame dead void, aligned the empty sockets with the cards that fill them, replaced an off-frame entrance slide with a scale, 4 hardcoded hex. Measured 0% edge bleed both aspects. | 5d7c82d | 2026-07-27 |
| mythfact | 19 | Text/general | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js card blocks removed, cards drawn as pixel rects directly (no camera/frustum math needed at all now that geometry IS the pixel layout). **Variant collapsed by ASPECT, not scene-id**: `split = !vertical` — long always side-by-side, short always stacked — same move as `stat`, satisfying one-look-per-kind and per-aspect composition (s10) together. Dropped the 3rd 'emphasis' variant (payoff card dominant); the two aspect-driven layouts read cleaner. **Caption-band regression caught before commit**: converting the bob amplitude from world to pixel units left it unreserved in the layout budget, intruding -3.5px/-6.9px into the caption band at both aspects (worst p=0.6, the bob's peak). Fixed by reserving `2x` the bob amplitude in `usableH` up front. 5/5 both aspects motion (unchanged from pre-rebuild); edge-check 0.0%; safe-check clear after the fix. | (uncommitted) | 2026-08-08 |
| dialogue | 1 | Text/general | passed | 3 | 4 | 4 | 4 | 4 | 4 | 1/1/2/1/1 -> 4/4/4/4/4. The scene rendered ZERO message text on every frame after the first: projectedPoints was a per-paint array filled inside build()'s update() closure, which captures frame 0's array, so the live one stayed empty and every bubble was dropped. Realigned the 3D layer to the 2D layout with an axis-aligned camera (projectToRect is affine on a z=const plane, so pixel<->world round-trips exactly); removed the two-axis group wobble. Also: accentSoft/rgba() into THREE.Color dropped alpha and rendered a solid sky-blue slab; edge wireframes now fade with their face; short threads centre instead of pinning to the bottom of an empty panel; 5 hardcoded hex. console.log clean, 0% edge bleed. | edb851b | 2026-07-27 |
| terminal | 19 | Text/general | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js window slab + glass sheet removed (was already correctly transform-free per the prior fix's own discipline), replaced with roundRect + applyElevation. departT wraps the scene. **Genuine occupancy defect found**: 1/5 both aspects (dead 56%, still 69-76%) — the type budget caps at 62% of the beat, so idle time is common, and the cursor blink alone (~15px wide) is far too small to register once typing finishes. Added a full-width breathing highlight band under the cursor's row via idle(); first attempt at 0.05 base alpha barely moved the number (median 0.002->0.016), 0.06-0.20 range lands 5/5 both aspects. edge-check 0.0%; safe-check clear. Verified visually via filmstrip: commands type in, output lines follow, breathing band visible on the resting prompt, clean exit. | (uncommitted) | 2026-08-08 |
| storyboard | 1 | Text/general | passed | 3 | 4 | 4 | 4 | 4 | 4 | 2/3/3/2/1 -> 4/4/4/4/4. Top-down camera + a 2D card rect taken from the bounding box of two projected corners: every slab keystoned while its card stayed axis-aligned, so margins differed per row, panels were unequal sizes, and the lit top faces read as silver bars. Pixel grid is now authoritative, each slab mapped onto its exact cell via an axis-aligned camera. Also clamped the grid above the Shorts UI band (bottom caption was at y=1690), removed the easeOutBack reveal scale that shrank 1.0->0.9 before growing back and the active-panel y-bob, one absolute card inset on all sides, 2 rgba()-into-THREE.Color calls + 8 hardcoded literals gone. console.log clean, 0% edge bleed. | 6963d39 | 2026-07-27 |
| question | 19 | Text/general | passed | 1 | 5 | 5 | 5 | 5 | 5 | Phase 19: already pure 2D from the prior rewrite (a88c120), only `variantOf(scene.id,3)` remained to collapse. **Genuine occupancy defect found**: short scored 2/5 (still 44%) — whichever of the 3 variants the current fixture hashed to lacked continuous motion once settled. Collapsed to the ring-pulse variant (expanding rings + rotating mark), the only one of the three with ongoing motion; the calmer underline mark and the static watermark are both gone. 5/5 both aspects; edge-check 0.0%; safe-check clear. Re-verified against the real corpus's longer texts (`--scene=recap/challenge/outro`, all three): 5/5 motion, 0.0% edge, both aspects, all three — the canonical variant holds at the text lengths that matter. departT added for a real exit. | (uncommitted) | 2026-08-08 |
| quiz | 19 | Text/general | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js panel + option slabs removed (roundRect + applyElevation for the panel and each row). departT wraps the whole scene. **Genuine occupancy defect found**: 2/5 both aspects (dead 36%, still 53-54%) — once the reveal beat settled, nothing continued to move for the remaining ~36% of the scene; the original 3D emissive intensity was static post-reveal too, so this predates the rebuild. Added a breathing glow+tint on the correct row via idle(), matching table/bullets/chart's fix. 5/5 both aspects; edge-check 0.0%; safe-check clear. Verified visually via filmstrip. Known probe limitation carried forward: the think-time countdown HUD needs a gap between beat windows the probe doesn't synthesize, so it remains unexercised by automated checks. | (uncommitted) | 2026-08-08 |
| code | 19 | Code | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js editor panel removed (was already correctly transform-free — no bob/scale/rotation, the prior fix's own discipline), replaced with roundRect + applyElevation. departT wraps the scene. Also removed 2 constants (`BLOCK_DEPTH`, `EDGE_ALPHA`) left dead by the removal. **Genuine occupancy defect found**: short scored 2/5 (still 42%) once typing paused mid-segment — the blinking cursor and focus-line rail are both too small individually to register at the motion-check grid. Made the full-width active/segment line-highlight band breathe via idle(), covering enough area to register. 5/5 both aspects; edge-check 0.0%; safe-check clear. Verified visually via filmstrip: syntax highlighting, traffic lights and typing cadence all correct, clean exit at p=1.0. | (uncommitted) | 2026-08-08 |
| trace | 19 | Code | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js code panel removed (was already transform-free per the prior fix), replaced with roundRect + applyElevation. departT wraps the scene, threaded through the tile/pointer alpha sites. Already scored 5/5 motion both aspects with no occupancy fix needed: the existing breathing highlight bar (idle-tied) and pointer bob already gave it real occupancy — this kind was one of the better-designed ones in the library going in. edge-check 0.0%; safe-check clear. | (uncommitted) | 2026-08-08 |
| memgrid | 19 | Code | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js slab removed; cells now fill directly in 2D using the exact face/emissive logic the removed material carried (writing=accent, occupied=secondary, idle=panel tint), so colour meaning is unchanged. departT wraps the scene. **Genuine occupancy defect found**: 2/5 both aspects (still 53-58%) — a static glow on occupied cells went fully still the moment a step's writes/frees finished, which is most cells for most of the scene. Made occupied-cell glow breathe via idle(), phase-offset per cell index so the whole grid doesn't pulse in lockstep. 5/5 both aspects; edge-check 0.0%; safe-check clear. Verified visually via filmstrip: values fill and glow correctly, pointer highlights the active index, clean exit. | (uncommitted) | 2026-08-08 |
| callstack | 19 | Code | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js slab removed; frames now fill directly in 2D with the same face/emissive logic the removed material carried (top=accent, idle=panel tint). Added breathing to the top frame's glow via idle() so it doesn't go still once a push/pop settles — same class of fix as memgrid, applied preemptively this time since the pattern was already known. departT wraps the scene. 5/5 both aspects motion on first pass; edge-check 0.0%; safe-check clear. Verified visually via filmstrip: push/pop animate correctly, top frame glows, stack unwinds, clean exit. | (uncommitted) | 2026-08-08 |
| bits | 19 | Code | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js register slabs removed; per-cell bit/flash/scale state (previously computed inside a per-frame-frozen closure) is now a plain loop each frame, drawing the exact same face-colour logic directly (flash=accent+glow tint, bit=accent tint, idle=panel lift). The removed 'card-flip' 3D effect (scale.x flip during logic/not ops) is kept as a 2D horizontal scale. departT wraps every draw call (cells, digits, shift overlay, operand row, chip, readout, note). 5/5 motion both aspects on first pass, no occupancy fix needed — the shimmer, flash and decimal-readout crossfade already gave it real continuous life; edge-check 0.0%; safe-check clear. Verified visually via filmstrip: bits set correctly, AND shows its operand row, shift animates the digit slide with an entering zero, readout updates, clean exit. | (uncommitted) | 2026-08-08 |
| browserframe | 19 | Code | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js browser panel + block slabs removed entirely. The camera was already on-axis and never rotated/bobbed (a prior fix's own discipline), so the projection was a pure affine map — replaced with direct pixel arithmetic (`spreadXpx`/`spreadYpx` in place of world-unit `spreadX`/`spreadY`), no camera or projectToRect left in the file at all. Blocks now fill directly with `roleFace`'s tint, which the 3D layer already computed but never got to show since it was hidden behind the 2D overlay. departT wraps every draw call. 5/5 motion both aspects on first pass; edge-check 0.0%; safe-check clear. Verified visually via filmstrip: chrome bar (traffic lights, typing URL caret, phase badges), skeleton blocks, painted card, footer all render correctly with no clipping. | (uncommitted) | 2026-08-08 |
| threads | 1 | Code | passed | 1 | 4 | 4 | 4 | 4 | 4 | `worldPos` mapped a task's start/len across the FULL frustum width regardless of `labelW` — the gutter reserved for "Thread A"/"Thread B" chips — so the earliest tasks projected under the same screen X as the lane labels and rendered on top of them. Measured concretely: "Thread A" and the "read" task label unreadable on top of each other, and a marker chip ("B blocks") clipped clean off the left edge, on the first render. The gutter math already existed (a `timeX` helper) but was dead code — nothing used it. Tasks now placed in pixels first inside that same band via an on-axis camera + `mappingAt`/`toWorld` (same technique as `table.ts`/`circuit.ts`); lane labels/task labels/clash burst/markers all pixel-native now too, no more projection round-trip. Marker chips clamped so one near either end of the track can't hang off the frame. Also removed a dead ground grid/shadow plane (would've rendered edge-on under the new on-axis camera); 7 hardcoded hex -> `THEME` tokens, including a `DANGER` constant that just re-typed `THEME.danger`. `edge-check` 0.0% both aspects, console.log empty. | af4ad5f | 2026-08-08 |
| cipher | 1 | Code | passed | 1 | 4 | 4 | 4 | 4 | 4 | The file's own STOPGAP comment already named the defect: rows at literal world Z under a camera elevated to (0,12,10)/(0,10,8), tuned for one `rect`, drifting the moment the caption-aware `contentH` changed aspect (measured: short 2.6% left bleed). 20th painter on the systemic `2d-layout-round-tripped-through-camera` pattern. Rows now laid out in pixels first (`pixelPos`, both hash's 3-row and shift's 2-row layouts), blocks mapped on via an on-axis camera + `mappingAt`/`toWorld`. The hash box's internal scrambling-digit grid was hardcoded to its OLD world size and overflowed the resized box — decoupled to a fixed unit-based `boxH`. Separate bug found verifying against the `mode:"shift"` fixture (only reachable via `--scene=`, so the richest-demo auto-pick never caught it): its reveal step is authored `op:"input"`, but `letterBeat` only recognized `op:"map"` — no letter ever revealed, any progress. Both ops now accepted. Also: `if (!cam) return` removed (2D is pixel-first, needs no camera); 7 hardcoded hex -> `THEME` tokens/derived shades. `edge-check` 0.0% both aspects on both fixtures, console.log empty. | 9ea2e8f | 2026-08-08 |
| circuit | 1 | Code | passed | 1 | 4 | 4 | 4 | 4 | 4 | The PROGRESS.md capture-crash note was already stale (the `hex.slice` bug was gone). Real defect found instead: parts sat at raw data x/y under a camera elevated to (0,12,10), so on-screen size depended on ground-plane foreshortening, not `rect` — the board filled the top third of the frame with a dead void below. 17th painter on the systemic `2d-layout-round-tripped-through-camera` pattern. Board is now laid out in pixels first (used extent centred/scaled to fill the frame) and parts mapped onto that grid via an on-axis camera + `mappingAt`/`toWorld` (same technique as `table.ts`/`diagram.ts`); also ported `diagram.ts`'s rotate-to-fit for a wide-on-short circuit on a 9:16 frame. Second bug found mid-fix: node depth was accidentally scaled by the same pixel-mapping factor as width/height, so every part was as deep as it was wide and showed a thick slab of side wall off-axis — fixed to a small fixed world thickness. Chip label clamped so a long label on an edge part can't hang off frame; 5 hardcoded hex (idle face/edge, chip panel) -> `THEME` tokens. `edge-check` 0.0% both aspects, console.log empty. | e1fb39e | 2026-08-07 |
| trafficflow | 1 | Code | passed | 1 | 4 | 4 | 4 | 4 | 4 | In horizontal layout, servers spread across world Z while the LB offset along X — the transpose of vertical mode's working axis assignment — which compressed under the fixed camera angle into a diagonal cascade where each server's label overlapped the previous one's load bar (confirmed visually). Fixed by using the same axis assignment in both orientations. Also fixed the algorithm label being filled the same color as the LB block it sits on (nearly invisible) and replaced 2 re-typed THEME.danger literals. console.log clean, 0.0% edge bleed both aspects. | 58a224b | 2026-08-08 |
| eventbus | 1 | Code | passed | 1 | 4 | 4 | 4 | 4 | 4 | palette.accentSoft (an rgba() string with alpha baked in) was passed into THREE.Color.setStyle() for topic blocks — THREE can't parse that alpha, so it silently dropped to full-strength RGB and logged a warning every frame (60 warnings per capture). Fixed by giving applyMat an opacity multiplier and using solid accent + reduced opacity for topics instead of an alpha channel THREE can't read. console.log clean (was 60 THREE.Color warnings), 0.0% edge bleed both aspects. | c3fd883 | 2026-08-08 |
| chart | 19 | Charts | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19, all 5 modes (bars/column/line/area/pie/donut). three.js removed from `column` — was the only 3D mode — rebuilt in pure 2D via roundRect + applyElevation (paint p90 ~unmeasured-was-3D -> 1.0ms). departT wraps the whole dispatcher; every mode's globalAlpha sites multiply by `leave` so it resolves on the hard cut. **Occupancy failures found and fixed on non-default fixtures** (`edge-check` and the old ledger row both only ever measured the default `bars` scene): `cht-pie` scored 2/5 both aspects (still 46%) with no continuous motion once slices settled -> added a breathing glow on the active wedge, matching the idle treatment bars/donut already had. `cht-area` long scored 2/5 (still 41%) -> the flowDots packet was gated behind `allIn` so a chart with 6+ points sat still until the last beat; ungated to travel the drawn portion throughout, plus a slow breathing opacity on the area fill itself (small-dot motion at 64x36 diff scale was imperceptible; a large filled region breathing is not). All 5 modes now 5/5 both aspects (verified per-mode via `--scene=cht-{bars,line,area,pie,donut}`); edge-check 0.0%, safe-check clear (both measure the default `bars`/`column` scene only). | (uncommitted) | 2026-08-08 |
| table | 19 | Charts | passed | 2 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js slab removed (cells fill via roundRect + applyElevation), departT wraps the whole scene. **Genuine occupancy defect found and fixed**: motion-check scored 1/5 short, 2/5 long (dead 25%, still up to 64%) — a static per-row tint left the table fully motionless from the start of the intro beat through row 1's entrance (the intro beat via `sayIntro` has nothing else happening at all). Added a continuous shimmer sweep across the active row (header during the intro beat, each body row once current) — 5/5 both aspects, dead 0%, still 0%. **Process note**: the fix's contact sheet appeared to show the departed frame (p=1.00) still fully rendered — traced with a temporary instrumented render and a pixel-cropped cell rather than trusting the downscaled thumbnail, per the runbook's own warning; the departure was correct and the strip was misread at reduced resolution, not a defect. edge-check 0.0%; safe-check clear. | (uncommitted) | 2026-08-08 |
| timeline | 19 | Charts | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js spine/blocks removed entirely. Camera was already on-axis so the world-to-pixel mapping was affine; replaced with direct pixel-space positions (no frustum/camera math needed at all now), keeping the ONE-shared-bob-for-slab-and-label discipline the prior pass established, just computed in pixels instead of world units then projected. Cards fill via roundRect + applyElevation. departT wraps the scene. motion-check 5/5 both aspects on first pass, no occupancy fix needed (existing bob + spine + current-card tint already gave continuous life); edge-check 0.0%; safe-check clear. Verified visually via filmstrip (vertical orientation). **Known gap carried forward**: only a vertical-orient fixture exists in the demo corpus, so the `orient: "horizontal"` code path remains unexercised by automated checks — same class of probe-coverage gap as quiz's think-timer HUD. | (uncommitted) | 2026-08-08 |
| ledger | 1 | Charts | passed | 2 | 5 | 4 | 5 | 5 | 5 | Reach 5 of 19 kits. Worker's pass fixed a real defect: portrait 3-party layouts pack cards close enough that a fixed `unit*2` bow (90px) was 2.5x the ~35px chord between two facing cards, drawing a detached loop past both — capped at `min(unit*2, chord*0.55)`. It also fixed 6 hardcoded hex, an absolute `lineWidth=1.5`, and the debit-party tint (`THEME.warn` -> `THEME.danger`, standard debit/credit convention). **A second, deeper instance of the same class survived that fix and was caught only by re-verifying visually at full zoom, not the downsampled strip: the transfer connector's anchor rect (`cw`/`ch`, a LABEL footprint sized for icon+name+amount text, `contentW*0.44` in portrait) has NO relation to the rendered 3D slab's own size (`blockW`/`blockD`, a much smaller world-space box under this file's tilted, off-axis camera).** The bow-cap fix made the arc's SHAPE correct relative to its own anchor points, but the anchor points themselves sat at the edge of the (much larger) invisible label zone, so the Bilal->Chetan connector still rendered as a small arc floating entirely clear of both visible cards — confirmed by direct coordinate logging (`a.x+a.w=522.28`, `b.x=557.72`, chord 35.44px, both far from where the slabs actually render on screen). **First hypothesis (idle-bob desync between the label's 67.5px bob and the arc anchor) was tested with the same logging and DISPROVED — the anchor floated even before any bob was involved.** Real fix: each party's arc-anchor is now derived from the SLAB's own projected on-screen half-extent (one extra `projectToRect` call per axis, `blockW/2`/`blockD/2` projected through the same camera), not the label rect — the connector now visibly touches both slabs at both aspects (verified: `short-p90.png`, `long-p90.png`). `cw`/`ch` are untouched for the label/chip text, which still needs its own room. MEASURED: edge-check 0.0% both aspects (unchanged throughout), safe-check full clearance both aspects (unchanged), tsc 0, console.log empty (debug instrumentation used during investigation was removed), hex grep 1 (a comment, not a colour). Residual: this file's camera remains tilted/off-axis with hardcoded world spans (`spreadX`/`spreadZ`) rather than the on-axis, pixel-authoritative pattern ~18 other painters now use — the slab-projection fix corrects THIS symptom without that larger rework, so a future change to block geometry or camera framing could reopen the same gap; flagged for whoever next touches this file's 3D setup. |  | 2026-08-07 |
| sankey | 1 | Charts | passed | 1 | 5 | 5 | 5 | 5 | 5 | Reach 5 of 19 kits. `branchTints` was already switched to `seriesTints` in an earlier pass — confirmed here it produces 4 visually distinct tints with no regression. The real defect: `branchPx` sized branch labels against a fixed FRACTION OF THE WHOLE FRAME (`vertical ? aw*0.25 : aw*0.15`), with zero awareness of a neighbouring branch — a single-label-fits-in-isolation check. Measured on the 4-branch demo (Rent 35 / Food 25 / Savings 20 / Everything else 20) at 9:16: "Savings"/"Everything else" and their identical "20%" badges overlapped into unreadable mush. Fixed with new `branchAxis`/`branchSlotPx`, which projects every branch centre once and measures the real pixel gap to its nearest neighbour; both the label and the pct/value row now cap against that gap (shrinking together, down to 55%, if still tight) instead of the frame fraction. Also closed a palette gap the seriesTints pass didn't reach: 4 more hardcoded hex (grid line, branch face, active face x2, travel dot) -> `THEME`/`shade()` — grep for hex literals **5 -> 1, and that 1 is a comment describing the earlier fix, not a colour value.** Measured: edge-check 0.0% both aspects (unchanged), safe-check full clearance both aspects (unchanged — fix was horizontal, not vertical), tsc 0, console.log empty. Residual, explicitly not fixed for lack of a repro: the neighbour-gap fix has a floor (unit*1.2 label width, 55% shrink), verified clean only at 4 branches — the richest demo data for this kind — so a hypothetical 6+ branch scene with very tight equal values could still collide; flagged for whoever adds a denser sankey demo rather than guessed at. |  | 2026-08-07 |
| gauge | 1 | Charts | passed | 1 | 4 | 4 | 4 | 4 | 4 | Rewritten by a parallel worker that was killed mid-refactor by a session limit, then repaired and verified here — so the scores are measured, not inherited. It died between introducing a family of dial radius constants and defining the label one, leaving `LABEL_R` undefined at :410-411 and the tree at 2 typecheck errors; both min/max labels now use the file's own `radialLabel()` anchor (:210), which places them outside the arc at the same offset as every other dial label and aligns them AWAY from it, instead of centring them back over the track. **Palette gap found and closed centrally:** the file carried `DANGER = "#f87171"` because `THEME` had `good` and `warn` but no third state — measured, **17 painters hardcode a red and 12 re-type this exact hex**, and `common.ts`'s own copy is a Business & Startups subject accent, not a semantic token, so nothing was reusable. `THEME.danger` added as a pure addition; gauge now holds **0 hex literals**. Verified: edge-check **0.0% both aspects**, tsc 0, console.log empty, 0 absolute lineWidth, save/restore balanced 7/7, no Math.random, `safeBottom` respected. Entrance strip (33ms cells) shows a real staggered cascade — arc sweeping by 33ms, zones 133-267ms, ticks staggered, needle from ~267ms, labels then legend last — so no pop-in and content resolves by frame 1. Needle overshoots and settles per beat. Residuals NOT fixed: the value readout holds identical over the last ~13% (p=0.87-1.00), and the value marker deliberately spans the track (`MARKER_IN_R` 0.9 -> `MARKER_OUT_R` 1.14), which reads as the needle poking through the arc because both are the accent colour. |  | 2026-08-05 |
| pictogram | 1 | Charts | passed | 1 | 5 | 4 | 5 | 5 | 5 | Reach 6 of 19 kits. Predecessor's rewrite replaced the whole three.js path with pure 2D — no camera, no `projectToRect`, no `makeBlock` — which removes systemic causes 1, 2, 4 and 5 BY CONSTRUCTION; reviewed and finished here. **The priority hypothesis was DISPROVED by measurement:** the glyph size already derived from the cell and provably fit its box (`cols*pitchX <= box.w` and `rows*pitchY <= box.h` proven for every `total` in a 13-value sweep x both aspects plus 2 degenerate boxes — **26/26 fits, 0 overflows**), so the zoomladder glyph-cap class was absent here. **The real defect was legibility:** the decade branch was dead code (decade glyph 58.1 vs unrestricted 72.6 = ratio 0.800, under a 0.86 tolerance), so `total=100` drew as **13 cols x 8 rows at 9:16 and 20 x 5 at 16:9** — a '65 of 100' card in rows of thirteen is uncountable, and the two aspects disagreed structurally. Column choice is now restricted to multiples of ten with a `DECADE_GAP` break: 10x10 and 2x10x5. **Frozen tail proven by A/B, not assumed:** the closing settle was anchored at `lastEnd + 0.04`, past the end of every beat window (probe beats end at 0.95, engine at ~1.0), so `finalT <= 0.033` at any p and the closing pulse plus the largest-group breathe never ran at all — the old line was temporarily restored to measure it: **0.005-0.007% of pixels changed across p 0.84->0.93**, i.e. pixel-frozen for the last ~16% of the scene. Now anchored to the last group's wave completion; tail motion **0.005% -> 1.0-13.1%** per sample. **Palette collision measured across all 18 subject palettes:** `groupTints` returned `[accent, secondary, THEME.good, shade(accent,0.5)]`, and Money & Finance's accent `#4ade80` **IS `THEME.good`, byte-identical — worst pairwise RGB separation 0**; Business & Startups and Mythology measured 20 from `THEME.warn`. Finance and Environment are both pictogram kits, so this shipped. Fallbacks are now distance-selected (`TINT_MIN_DIST 60`) from `[good, warn, danger, text]`, skipping any too close to that subject's accent/secondary: worst separation **0 -> 64**. Also fixed: the pop lift was applied after seating so the arc's top ring rose **17.3px above `box.y`** into the 13.5px title gap (now clamped); the 9:16 legend's two columns had a 0.54u gutter so a right-aligned '65' sat 24px from the NEXT column's dot and read as its value (gutter 1.2u); and a duplicate shadowing `fieldIn` removed. MEASURED: edge-check 0.0% both aspects, tsc 0, console.log empty, 0 hex, 0 absolute lineWidth, save/restore 3/3; pixel bbox clears `safeBottom` by **25px (short) and 38px (long)**. Residuals: arc mode has NO demo anywhere in `demo.ts`, so its majority line and pill are proven contained by clamps and a numeric sweep but never rendered; the 3-4 group legend is arithmetic-only for the same reason; and in 9:16 the legend's right edge reaches x=960 of 1080, inside YouTube's ~15% action rail — that follows `makeLayout`'s `contentW` (which runs to x=1017) and is systemic to the layout, so it was left rather than fixed unilaterally. |  | 2026-08-06 |
| race | 1 | Charts | passed | 1 | 5 | 4 | 4 | 4 | 5 | **The last kind in the library with a measured containment failure (long 2.2%, right 2%).** Root cause was the canonical one: a pixel chart round-tripped through a tilted `isoCamera()` with an invented +/-5.5 world span, then labels placed by raw unclamped `projectToRect()` — at 16:9 a full-length bar's tip projected to x~1796 in an 1857-wide content box and its trailing value label ran to ~2120px on a 1920px frame; at 9:16 the second racer's row projected to x=-404. Rewritten to a pixel-authoritative layout with an on-axis camera at (0,0,12), never scaled/rotated/bobbed, `worldX`/`worldY` converting pixels->world by one uniform scale, slabs at `z=-depth/2` so the front face lands on the focus plane. Also fixed a crash-class bug: the old `if (!cam) return;` returned BETWEEN `ctx.save()` and `restore()`, which is the unbalanced-state failure that poisons every later frame of a video; replaced with a flat 2D fallback. **Three defects found by measurement after containment was already clean:** (a) the 3D slab depth was unconditionally `thick*0.85`, and because the back face projects toward the frame centre a 10px bar carried **25px of silhouette overhang, 3.5x overstated**, plus 12px of vertical overshoot above its rail — depth is now capped at the bar's own length; (b) `gmax` is global across checkpoints, so early checkpoints scale to **0.11px and 0.037px** — the depth artifact had been the only thing making them visible, so non-zero bars are now floored at `unit*0.22`; (c) the lane rail's hair stroke was centred on `safeBottom` in the many-racer clamp case — showdown's exact bug — now inset by half a stroke. MEASURED: edge-check **2.2% -> 0.0% both aspects**, tsc 0, console.log empty, 0 hex, 0 absolute lineWidth, save/restore 8/8, no Math.random, no `rotation.`. Entrance strip at 33ms/cell shows the 90ms lane cascade arriving cell-to-cell with nothing going 0->full between adjacent frames. **Two contact-sheet readings were wrong and are recorded so they are not re-litigated:** the 16:9 layout looked like it used only the top third but measures `listTop + 2*laneH = 863.5` against a computed `safeBottom = 863`, i.e. it fills the band exactly; and frames at p=0.27/0.33 and 0.53/0.60 looked frozen but are the post-settle hold of each beat with the leader's `idle(env,2200)` emissive still running. Residuals: with only 2 racers `barThick` caps at `unit*2.4` against a ~3x larger `laneH`, leaving ~18% of frame height of air between lanes; the linear global scale parks two values 15x apart at the bar floor for the first ~2/3 of the demo, so bars carry presence and labels carry magnitude; and at 16:9 the checkpoint marker draws twice in the top-right (an 18%-alpha watermark plus a crisp chip beside it), reading as duplication rather than hierarchy. |  | 2026-08-06 |
| radar | 1 | Charts | passed | 1 | 4 | 4 | 4 | 4 | 4 | Row's old note ("CAPTURE CRASHES: cy is not defined") is stale — that crash was fixed in phase 1.2; this is the first actual visual review since the painter started rendering at all, and it is the joint-widest-reach unfinished kind (12 of 19 kits). Rewritten by a parallel worker, verified here after its run was cut short. **Measured: edge-check 0.0% both aspects, tsc 0, console.log empty, 0 hex literals, 0 absolute lineWidth, save/restore balanced 5/5, no Math.random, and it derives its area from `safeBottom` (:70 `ah = safeBottom - ay`) rather than contentH.** Notably it carries **zero `rotation.` calls**, so the `2d-layout-round-tripped-through-camera` mode that accounted for 17 earlier painters cannot apply here. Motion verified over p: the ghost web enters, then each series traces its polygon progressively (series 1 across p=0.07-0.47, series 2 p=0.53-0.80) with the legend row for the active series brightening as its polygon draws, and `settleIn = easeOutCubic(revealT(env, 0.78, 0.96))` (:62) lifts the past polygons and legend rows through the last quarter. **Two of my own first readings were wrong and are recorded so they are not re-litigated:** the 9:16 lower band looked like a large dead void but `R` is width-limited at that aspect (`availW/2 - gutter` binds, not height), so the space below the legend is the mandated caption reserve, not waste; and p=0.80-1.00 looked frozen in the downsampled strip but the settle resolve is genuinely running, just subtly. Residual: at 16:9 the web+legend group leaves the right ~20% of the frame empty, and the inner value labels (`40`, `70`) sit on the web's grid lines rather than clear of them. |  | 2026-08-05 |
| buckets | 1 | Charts | passed | 1 | 5 | 5 | 4 | 5 | 5 | Reach 4 of 19 kits. Containment/typography already clean. Same console-warning class as `skyline` this batch: 7 `rgba(...)`/hex literals fed into `makeBlock`'s face/edge params were consumed via `new THREE.Color(...)`, which drops alpha and logged **54 warnings** — `console.log` must be empty, so this was gate-blocking on its own. All 7 routed through `THEME`/`shade()` instead; console.log 54 lines -> empty. Also found the `makeBlock` edge-parenting bug (cause 5) live here: the "highlight the actively-filling bucket's glass rim" feature searched `container.children` for a `LineSegments`, but `makeBlock` parents edges to the FACE MESH not the group, so `container.children` was always just `[mesh]` — the highlight was dead code, confirmed by the fix now showing a visibly bright rim on the active bucket vs a barely-visible one on idle buckets (`short-p50.png`). All three buckets also shared ONE scalar entrance (`ghostIn`), i.e. simultaneous not staggered; now `stagger(i,n)`-delayed per bucket, verified by sampling label brightness across the entrance strip (Needs > Wants > Savings ordering at 200ms and 300ms, matching the intended delay order). 2 absolute `lineWidth=1` -> `unit*STROKE.hair`; two mismatched chip corner radii unified to `RADIUS.md`. Residual: stagger magnitude is real but modest (140ms spread across 3 buckets) — not a dramatic cascade. |  | 2026-08-07 |
| basket | 1 | Charts | passed | 1 | 4 | 4 | 4 | 4 | 4 | Cards sat on a ground plane at a fixed world spread under a camera elevated to (0,12,10), so on-screen size depended on that plane's foreshortening, not `rect` — filled a small patch near the top of both aspects, dead void below. 18th painter on the systemic `2d-layout-round-tripped-through-camera` pattern. Grid now laid out in pixels first, cards mapped onto it via an on-axis camera + `mappingAt`/`toWorld`. Separate real bug found comparing before/after: the price-tag flash did a boolean colour switch instead of interpolating by magnitude, so any nonzero residual — down to a floating-point sliver at the very last frame — rendered full warn colour; a card stayed solid yellow for nearly the whole tail. Fixed to `lerpColor` by magnitude. Rebuilding around the new, much taller card (fills its cell vs. a thin ground slab) surfaced two more: label collided with the card at every cell (explicit icon/label/card/tag vertical stack fixes it), and that stack's fixed zones could exceed a short cell's height on a sparse 16:9 grid (4 items at 3 columns), overflowing into the row below — fixed with a `zoneScale` factor shrinking the whole stack together. Also removed the ground grid + shadow plane (horizontal, rendered edge-on under an on-axis camera); `if (!cam) return` replaced with a flat 2D fallback so a WebGL failure no longer blanks the total too; 4 hardcoded hex -> `THEME` tokens. `edge-check` 0.0% both aspects, console.log empty. | bab9233 | 2026-08-07 |
| probability | 1 | Charts | passed | 1 | 4 | 4 | 4 | 4 | 4 | `isoCamera()`'s position was only ever overridden for the vertical case, so a horizontal `rect` (full content height, not square-ish like vertical) fell through to a position tuned for something else — measured concretely: the wheel clipped clean off the left edge on 16:9, not just underfilled. 22nd painter on the systemic `2d-layout-round-tripped-through-camera` pattern. Distance along the same iso viewing direction now solves analytically (same technique as `orbit.ts`): tan(halfFov)*D approximates the visible half-extent at the origin, so D solves for the wheel filling `FILL_FRACTION` of whichever frustum axis is tighter. 7 hardcoded hex -> `THEME` tokens / named constants matching `cipher.ts`'s convention. `edge-check` 0.0% both aspects, console.log empty. | b6f9a62 | 2026-08-08 |
| diagram | 19 | Diagram | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js slab removed (paint p90 21/22ms -> 1.3/1.1ms, ~17x), 3-shape variantOf (rounded/pill/hex) collapsed to rounded only, departT wraps the whole scene (nodes/ghosts/arrows/flow-dot/labels all fade on the leave alpha) so it resolves before the hard cut instead of vanishing. motion-check 5/5 both aspects unchanged from pre-rebuild (dead 19%/19%, still 20%/22%, events 7/7); edge-check 0.0%; safe-check clear. Verified visually via filmstrip: nodes fill correctly, ghosts precede reveal, frame 15 clears fully. | (uncommitted) | 2026-08-08 |
| tree | 19 | Diagrams | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js slab removed, nodes fill directly via roundRect + applyElevation (no camera/frustum math left at all — the layout was already pure pixel-space from the prior pass). departT wraps the scene, alpha threaded through connectors/dots/nodes. **Genuine occupancy defect found**: 1/5 both aspects (still 64-73%) — once a level's nodes finished popping in, nothing continued to move; the active node's border glow alone covered too little of the frame. Added a breathing fill tint via lerpColor+idle, matching table/bullets/quiz/steps' fix. 5/5 both aspects; edge-check 0.0%; safe-check clear. Verified visually via filmstrip: levels fill top-down, connectors draw on, leaf row ghosts before reveal, clean exit at p=1.0. | (uncommitted) | 2026-08-08 |
| mindmap | 19 | Diagrams | passed | 1 | 5 | 5 | 5 | 5 | 5 | Phase 19: already pure 2D (no three.js) and already only one composition (no variantOf) — nothing to collapse. Was already 5/5 on motion-check with no changes: continuous flowDots along branches once settled already gave it real occupancy the class of kind this phase targets usually lacks. **Only gap was departure** — no exit existed at all, so it would vanish at the hard cut; added departT threaded through the link strokes, the flow dots and every node's own alpha. Unchanged 5/5 both aspects after; edge-check 0.0%; safe-check clear. | (uncommitted) | 2026-08-08 |
| statemachine | 19 | Diagrams | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: the three.js layer was entirely redundant — the 2D chrome already fully rendered every state and edge on top of it, so the decorative slab behind was removed with no visual loss. **Variant collapsed**: the per-edge `variantOf(key,2)` curve-side coin flip removed, one canonical bow direction. departT wired by multiplying it into the single shared `introIn` factor every alpha site already reads, rather than touching each site individually. **Found and fixed a severe pre-existing bug while in the file**: `const key = \`\${e.from}>\${e.to}\`;` had an escaped `\$`, so the interpolation never ran and EVERY edge collided on the identical literal map key `"${e.from}>${e.to}"` — a multi-edge machine could only ever store one cached curve. Same escaping bug hit 3 `ctx.font` calls here (font silently stayed whatever was last valid) and, on a repo-wide grep, `lifeline.ts` (4 sites, already-passed row amended) and `cycle.ts` (3 sites, fixed when that kind's turn came). **Second defect found by measurement**: after the key fix, safe-check caught -10.5px at p=0.6 — the current-state ring's outward pulse (`unit*0.67` max) crossed safeBottom on a bottom-row state; fixed by reserving that margin in the grid's area height. 5/5 motion both aspects; edge-check 0.0%; safe-check clear after the margin fix. Verified visually: edges now curve individually instead of colliding, labels sized correctly, clean exit. | (uncommitted) | 2026-08-08 |
| cycle | 19 | Diagrams | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js layer removed entirely — the tilted-camera perspective it rendered from was replaced with a flat 2D circle (`posOf` is now a direct `cos`/`sin` pixel position, no camera/projection). Nodes now fill a disc directly (`idleFace`, matching the removed cylinder's idle tint) with elevation for depth. departT wired into every alpha site. First motion-check pass: long aspect 4/5 (events=1) — a per-node ambient bob at 0.1 unit amplitude inflated the median enough to drown the real arc-growth events, same two-sided calibration lesson as `quote`; reduced to 0.04 units, 5/5 both aspects. edge-check 0.0% both; safe-check clear both. Verified visually via filmstrip: arcs draw progressively with a flowing dot, active node glows and pulses, label chips stay clear of both edges and the caption band, clean exit. Repo-wide template-literal escaping bug scan (found while polishing `statemachine`) does not appear in this file — already clean. | (uncommitted) | 2026-08-08 |
| chain | 1 | Diagrams | passed | 1 | 4 | 4 | 4 | 4 | 4 | Reach 11 of 19 kits and the main cause-and-effect visual. Rewritten by a parallel worker, verified here after its run was cut short. **It replaced the entire 3D path with 2D** — `render3D` and `rotation.` both now count **0**, so neither the `2d-layout-round-tripped-through-camera` mode nor the frame-0 closure freeze can apply to this kind any more. Measured: edge-check **0.0% both aspects**, tsc 0, console.log empty, 0 hex literals, 0 absolute lineWidth, save/restore balanced 6/6, `safeBottom` respected. The worker's two stated leftovers were both already done in the code it left: labels render single-line, and the ghost skeleton **cross-fades** rather than blinking — `gi = ghostT(i) * (1 - appearOf(i))` (:251), so the dashed outline's alpha falls exactly as the solid card rises, which is the axis-3 "continuous" requirement (nothing may exist at frame 8, vanish at 9 and return at 10). Verified over p: ghosts for all 4 links present from the start (so no pop-in), each card resolves in turn with its numbered badge, connector arrows draw and terminate ON the next card's border with no overshoot, and at 16:9 the layout switches from a vertical stack to a horizontal zigzag rather than squeezing. **Strip-reading caution recorded:** the settled cards appear to have dashed borders in the downsampled contact sheet, but the painter contains **zero `setLineDash` calls** — it is an aliasing artifact of a thin border at 26% scale, not a defect, and should not be "fixed". Residuals: at 16:9 the four cards leave the upper and lower thirds of the safe area fairly empty rather than scaling up into it, and unarrived ghost cards carry no icon so they read as blank boxes. |  | 2026-08-05 |
| lifeline | 19 | Diagrams | passed | 1 | 4 | 4 | 4 | 4 | 5 | 1/3/3/1/2 -> 4/4/4/4/5. Every 2D point was round-tripped through a camera at (0,15,12) by `getProjPos`, so **the three actor chips were pulled toward the vanishing point until they overlapped** ("Auth server" was covered by "API"), **the lifelines splayed outward as they descended** and drifted away from the chips that name them, and the message arrows landed short of their own lane. The 2D layer is now pixel-true: chips sit on their lane, lifelines are true verticals, arrows terminate exactly on them. **The 3D layer is gone** — the server racks and database stacks were placed by mapping pixels into world space, so they could not register with a pixel lane; they sat between the lanes, overlapped the message rows, and one projected a bar off the left edge. They carried nothing the actor chips do not already say (each chip has the actor's icon). Separately, **the activation bar was drawn by rotating to `atan2(dy, dx)`**, so when an actor's first message came after the current step, dy went negative and the bar was drawn horizontally off the left edge of the frame — it is a plain vertical rect now. `h * 0.86` -> `layout.safeBottom`. 11 colour literals -> 0 (including `rgba(148,163,184,0.5)` into `studioLights`, alpha dropped). Message-label size 0.62u -> 0.72u; still the smallest text in the frame, recorded as the one thing left. `edge-check` 0.0% both aspects, console.log empty. | b06efea | 2026-08-01 |

**Follow-up (same session):** a repo-wide grep for the pattern found `ctx.font` template literals with an escaped `\${...}` — the interpolation never ran, so `ctx.font` was set to the literal invalid string `"700 \${labelPx}px \${FONT_SANS}"` and canvas silently kept whatever font was last validly set. Fixed 4 occurrences here (labels, icon, message text). Not caught by any automated check — font size isn't something edge-check/motion-check/safe-check measure — only found by re-reading the file while fixing the same bug in `statemachine.ts`. Re-verified visually via filmstrip: actor and message labels render at the correct size.
| geomap | 1 | Diagrams | passed | 1 | 4 | 4 | 4 | 4 | 4 | studioLights' rim-light color was an rgba() string with alpha — THREE.Color.setStyle() can't parse that, silently dropped it and logged a warning every frame (6 per capture, same bug class as eventbus.ts). Passed the existing solid THEME.textDim hex instead (also the semantically correct token). console.log clean (was 6 THREE.Color warnings), 0.0% edge bleed both aspects. | 090f013 | 2026-08-08 |
| layers | 1 | Diagrams | passed | 1 | 4 | 4 | 4 | 4 | 4 | studioLights' rim-light color was an rgba() string — THREE can't parse the alpha, silently dropped it and logged a warning every frame (same bug class as eventbus.ts/geomap.ts). Fixed with solid THEME.textDim. Also: the demo fixture titled "OSI 7 Layer" only defined 4 layers — added the 3 missing (Presentation, Session, Data Link) in demo.ts; painter itself handles any layer count generically, confirmed by re-rendering with all 7. console.log clean (was 6 THREE.Color warnings), 0.0% edge bleed both aspects. | 3d45814 | 2026-08-08 |
| orbit | 1 | STEM | passed | 1 | 4 | 4 | 4 | 4 | 4 | Two real defects found by rendering, not assumed. (1) Camera distance was a hand-tuned constant multiplier unrelated to `rect` — system filled under half the frame with dead space on every side. Solved analytically: tan(halfFov)*D approximates the visible half-extent at the origin (same relation `frustumHalfExtent` uses on-axis), so distance now solves for the outermost ring filling `FILL_FRACTION` of whichever axis is tighter. (2) Labels track independently-orbiting bodies forever, so at some point any two are bound to project to the same spot — measured concretely: Sun/Venus and Earth/Mars overlapping into unreadable text at p=0.9 on the first render. Chips now placed in priority order (fixed centre, then active body, then rest) with a per-frame nudge off anything already placed; non-active chips also dropped to 0.75 alpha. 3 hardcoded hex -> `THEME` tokens. `edge-check` 0.0% both aspects, console.log empty. | 1f0e503 | 2026-08-08 |
| schematic | 1 | STEM | passed | 1 | 4 | 4 | 4 | 4 | 4 | The "arch" shape rendered as a solid dome, not an arch — confirmed by rendering the "A simple arch bridge" demo, whose title is exactly what the shape failed to look like. The generic per-part `d` (depth, meant for a block's Z-extent) was also read as the torus TUBE diameter; at d=w*0.4 the tube radius rivaled the ring radius, so the arch was a solid ball with barely a hole. `archD` gives it its own, much shallower thickness. Also: idle parts used a near-black "#1e293b" for both albedo and emissive, reading as almost invisible against the background under this scene's studio lighting — swapped for the `shade(THEME.panel, 0.09)` idle-face convention `table.ts`/`bits.ts`/`circuit.ts` already use. 3 more hardcoded hex -> `THEME` tokens; removed an unused `makeBlock` import. `edge-check` 0.0% both aspects, console.log empty. | 9cb6c7c | 2026-08-08 |
| terrain | 1 | STEM | passed | 1 | 4 | 4 | 4 | 4 | 4 | Camera/world-size already correctly handled for both aspects (explicit vertical/horizontal branches, not a one-sided fix like `probability.ts` had) — measured 0.0% edge bleed both aspects before this change. 12 hardcoded hex -> `THEME` tokens / named constants matching `cipher.ts`'s convention and `table.ts`'s idle-face `shade(THEME.panel, 0.09)` pattern, including the terrain surface's own base color/emissive. Two pure-white glow literals for snow/ice highlights kept — same accepted convention `bigtext.ts` (passed) uses. `edge-check` 0.0% both aspects, console.log empty. | 47ce2ad | 2026-08-08 |
| zoomladder | 1 | STEM | passed | 2 | 4 | 4 | 4 | 4 | 4 | Worst edge bleed in the library: **short 43.5% (bottom 43/left 30/right 14), long 12.1% -> 0.0% both aspects**. **Round-1 note corrected: the bleed was NOT intentional.** It read as "Earth filling the frame" but what overflowed was the rung ICON — a glyph sized `min(half*0.9, stageMin*1.4)`, i.e. up to 1336px on a 1080px frame — centred on a raw `projectToRect` through a camera that is mid-zoom, plus scale chips and labels at unclamped projected corners (a `1 m` chip half off the left edge, a person glyph in the title band). Plan §6d names this bleed as a defect to repair, so the row's "do not clamp" lost to the spec. Glyph now caps against the stage and its centre clamps (new `clampRange` in common.ts); chips clamp; neighbours are CULLED not clamped — clamping them first traded the bleed for an overlap failure (both are axis 1) by stacking them into the breadcrumb. Floating per-rung labels deleted: rungs nest, so a label under one glyph necessarily lands on its neighbour's ("Earth" on the city card) and `drawCaption` already prints the active scale+label at a fixed spot. Caption was at y~1700 inside the Shorts UI band; the stage is now `safeBottom`-bounded so it clears it. **The "hard black box seam" was the outermost rung's own slab** — each step out is 6.5x wider, so the rung one step outside active is a ~25-unit block 5 units from the camera, rendering as a frame-filling wall that the secondary studio light painted a magenta blob onto and whose bevel was the grey streak. Two wrong hypotheses first (grid/plane border, then far-plane clip: enlarging 100->400 changed nothing); fixed by fading rungs out by apparent size. Found in passing: `makeBlock` parents edges to the FACE mesh, so `r.mesh.children[1]` was always undefined and the active-rung highlight had never run. Frozen tail (4 identical frames p=0.80-1.00) fixed with a push across the whole beat. 0 hex literals, 0 absolute lineWidth, console.log empty. Residual: the slab's bottom edge line grazes the caption's second line at 9:16. | 141526b | 2026-08-05 |
| bodymap | 1 | STEM | passed | 1 | 4 | 4 | 4 | 4 | 4 | Figure was a fixed world size (~7.2x14.5) under a camera fixed at (0,0,20) with no relation to `rect` — filled a small patch near the top on 9:16, shrank to a centred thumbnail on 16:9. 19th painter on the systemic `2d-layout-round-tripped-through-camera` pattern. `frustumHalfExtent` now gives the space actually available for the rect's aspect; figure + mark positions (both derived from the same `regionPoint3D` anchors) scaled to fill 82% of whichever axis is tighter — a uniform-scale variant of the fit-to-frame idea `bits.ts`/`chart.ts` use via a pixel grid. Also: `if (!cam) return` replaced with a fallback camera for 2D projection so a WebGL failure drops the silhouette but not the marks/labels (the actual lesson content); ground grid repositioned/rescaled to stay under the resized feet; 5 hardcoded colour literals -> `THEME` tokens. `edge-check` 0.0% both aspects, console.log empty. | 65ff7d1 | 2026-08-07 |
| constellation | 1 | STEM | passed | 2 | 5 | 4 | 5 | 4 | 5 | Reach 7 of 19 kits. Prior pass had already removed the group Y-rotation and its mirrored `applyEuler` (systemic cause 1) and moved per-frame state into `render3D`'s `context` (cause 6); this pass finished and measured it. **The 16:9 star field measured 458x340px inside a 1794x514px box — 4% of frame** — because the layout still round-tripped through a +/-1.2-unit depth field costing a global 9% worst-case perspective margin, on top of a 1.5 anisotropy cap. Flattening z to 0 (camera on-axis, mesh scale IS `pops`, so the 2D rim and line math became exact rather than approximate) and raising the cap to 2.0 took the field to **692x413px, +51% width / +21% height**; 9:16 measures 917x800 in a 954x895 box. Design note: an exact per-star perspective solve was the runner-up and lost — it adds ~15 lines of math to recover the same 9% while KEEPING the distortion source that the systemic list names as the root cause in 17 painters. **Finale chip glow measured ~1346px against safeBottom 1321.5 — a 24px overrun into the burned-in caption band**; chip font/pad/glow are now named constants and `FINALE_BAND_UNITS` is DERIVED from them, so the reserve can no longer drift out of sync with the chip it reserves for. Measured clearance is now 24px at BOTH aspects, glow included. Lit stars rendered DARKER than the accent line joining them (emissive 0.12, metalness 0.3) — now emissive 0.5/0.95 at metalness 0.1, so a star is the brightest thing on a star map; unlit stars 6.4px -> 9.5px radius so they stop reading as sensor dust. Per-star bob/twinkle/breathe phases moved off the array index onto `hash01(id)`, keeping the 2D and 3D layers byte-identical and never touching `Math.random`. `if (!cam) return` now restores `textAlign`; the other early return already did. MEASURED: edge-check 0.0% both aspects before and after, tsc 0, console.log empty, 0 hex, 0 absolute lineWidth, save/restore 6/6, `rotation.` 0. Entrance strip at 33ms/cell shows a 70ms-staggered per-star arrival at both aspects with nothing popping in whole. **Two contact-sheet readings were disproved by pixel measurement** — an apparent top-third layout and an apparent 3:1 stretch both measured out as nothing, and the 9:16 lower void is the mandated caption reserve. Residuals: at 16:9 a tall point set is height-bound by aspect fidelity and still leaves ~600px of void each side (closing it needs anisotropy >2, at which the figure stops reading as the same shape); and `LABEL_BOX_UNITS 0.9` reserves label space at the top of the field even when only one point carries a label, costing ~11% of 16:9 field height. |  | 2026-08-06 |
| dayclock | 1 | STEM | passed | 2 | 5 | 5 | 4 | 5 | 5 | Reach 6 of 19 kits. Row's old note ("CAPTURE CRASHES... .geometry off a THREE.Group") is stale from before phase 1.4 fixed that crash — measured false before touching the file: the pre-existing stopgap rendered cleanly, no console errors. **The stopgap itself (R hardcoded to 3.1, tilted camera at (0,10,8), left by the orchestrator after the caption-boundary fix shortened this painter's stage) is now the real systemic conversion — the 18th painter on this pattern.** Camera moved on-axis to (0,0,D), never rotated after (`camera.rotation` count 0); dial radius R is now DERIVED from the rect (`contentW`/`availH`, itself already `safeBottom`-bound) rather than a hardcoded world span, mirroring gauge.ts. 2D overlays (numbers, icon, chip, readout) switched from `projectToRect` to a local `pixelAt()` affine map sharing the exact `(cx,cy,R)` the 3D group scales from every frame, so pixel math and the 3D dial can never disagree. Ground `GridHelper`/`ShadowMaterial` removed — horizontal props render nothing useful under an on-axis camera (same precedent as browserframe.ts). **safe-check FAIL -86.5px (short) / -157.9px (long) -> PASS/clear both aspects (root cause: the digital readout and pin icon were positioned from an UNCLAMPED world offset past the dial edge — R now budgets for icon+readout reach via `ICON_R_MULT`/`READOUT_R_MULT`, and the icon position is clamped via the existing `clampRange` helper as a backstop).** edge-check unchanged at 0.0%/0.0% throughout. 0 hex literals (was ~10), 0 absolute lineWidth, tsc 0, console.log empty. Verified the makeBlock/makeCylinder children[1] edge-parenting bug does NOT apply here — dayclock never indexes into baked edge highlights. Residual: ticks/rim/hub/hands render at full opacity from the first visible frame (~33ms) while only the base disc grows via `faceIn` — unchanged from the original stopgap, not introduced by this pass, and the dominant beat-to-beat motion (hand sweep, pin drop-in) is smooth throughout. |  | 2026-08-07 |
| geometry | 1 | STEM | passed | 2 | 4 | 4 | 4 | 4 | 4 | 3 confirmed bugs: rgba(palette.accentSoft,0.4) fed an already-rgba string into rgba()'s hex.slice(1), silently producing rgba(0,0,0,0.4) — the triangle's "Area" fill rendered as a near-invisible black wash; fixed with palette.accent (solid). The "90°" bisector averaged raw angles ((a1+a2)/2), which breaks across the wraparound and floated the label disconnected from its own right-angle mark; fixed by averaging unit vectors instead. Segment labels ("c = 5" on the hypotenuse) offset by a fixed vertical -12px still ran a diagonal line through the text; fixed with a true perpendicular offset + edge-aware alignment. A 4th attempt (nudging the fill's centroid label away from a co-located angle vertex) just traded one collision for another and was reverted — "Area"/"90°" crowd slightly on a very compact render, a residual lower-severity cosmetic gap. console.log clean, 0.0% edge bleed both aspects. | 10dd73e | 2026-08-08 |
| numberline | 1 | STEM | passed | 1 | 4 | 4 | 4 | 4 | 4 | Already clean — no fix needed. Verified tick math (irregularly-spaced 1.7/3.3/5/6.7/8.3 labels are the correct rounding of an evenly-divided 0-10 range, not a bug), zone shading, jump arc, and both aspects (9:16 frames a tighter crop of the same 0-10 line, 16:9 shows the full range — both correct, no bleed). Axis/tick 3D literals are a self-consistent neutral family, left as-is (same exception class as eventbus.ts). console.log clean, 0.0% edge bleed both aspects. | (no code change) | 2026-08-08 |
| molecule | 1 | STEM | passed | 1 | 4 | 4 | 4 | 4 | 4 | Verified both modes with real renders: "structure" (3D ball-and-stick, forced via --scene=mol since the kind's default demo picks "equation") is correct — CPK colors, bonds, labels all right. "equation" mode's sparse composition matches other text-centric kinds' minimalist layout, not a bug. Only fix: bondColor re-typed THEME.textDim. console.log clean both modes, 0.0% edge bleed both aspects. | ff1dfe7 | 2026-08-08 |
| formula | 1 | STEM | passed | 1 | 4 | 4 | 4 | 4 | 5 | 2/3/3/2/2 -> 4/4/4/4/5. **Every term symbol floated above the tile it names.** Tiles were slabs spread in world space and viewed from (0,12,10), while the symbols are laid out by TEXT FLOW — measured glyph widths, operators interpolated between terms — so the two were never the same row; the symbol also carried a deliberate `unit * 1.5` lift off an already-wrong projected centre. **The 3D layer is gone**: the tile is decorative, a panel behind a glyph, so it is drawn in 2D at the glyph's own position where it cannot drift. The active term is a tinted panel with an accent border (it used to be a full-`accent` slab). **Operators were placed between panel CENTRES**, which put the "×" inside the much wider `(1+r)ⁿ` panel — widths are now measured in their own pass first and each operator sits in the gap between the two panels' edges. `contentH` -> `layout.safeBottom`: the result line "1000 × 1.61 = 1,610" was drawn at ~85% of frame height, under the caption. 1 absolute-px stroke -> `STROKE`; 7 colour literals -> 0. `edge-check` 0.0% both aspects, console.log empty. | 4de39f6 | 2026-08-01 |
| curves | 1 | STEM | passed | 2 | 5 | 4 | 4 | 5 | 5 | Reach 7 of 19 kits. Predecessor's full 3D->2D rewrite (deleted the tilted camera at (1.5,6,8), the GridHelper, the ShadowMaterial plane, ~194 per-sample sphere meshes and every `projectToRect` call) was structurally correct and complete; this pass fixed what it introduced. **Orchestrator hypothesis CORRECTED: `enterT` was NOT the bug.** It appears exactly twice, both on absolute-time frame furniture (`frameIn`, `gridIn`), which is what its docstring says it is for; the curve draw-on is `drawProg = f(beatFrac(offset+i).t / DRAW_SHARE)`, a fraction of the curve's OWN beat window, so it was already duration-aware — a 20s beat draws over 11s, not 400ms. The real timing defect was the EASING CURVE, not the clock source. **Two hard discontinuities measured in the plot box:** lit pixels collapsed **55,584 -> 22,930 (-59%) between two adjacent samples** at p=0.345->0.350, because the active curve's glow was gated on a boolean `isActive` so `shadowBlur` over an 870px stroke switched off in the single frame the next beat opened, and the dashed ghost returned early in the same frame; and at every beat start the leading head-dot rendered as **a bare 140px disc with no stroke behind it, held from p=0.055 to ~0.090 (~875ms at 25s)**, because `easeInOutCubic` spends the first fifth of a beat at sub-pixel length while the head's alpha was already 1.0. Fixed with a continuous `beatPos`/`focusOf` emphasis ramp (0.35-beat handoff) replacing the boolean, `easeOutCubic` draw-on, a ghost that fades out UNDER the advancing stroke, and a head gated at both ends on radius AND alpha. **Worst beat-boundary step -59% -> -2.1%; first-frame-of-beat curve pixels 140 -> 676.** Full-res also caught what the contact sheet could not: the `Demand` chip lay ON the line it names and the `Equilibrium` badge sat on top of both curves at the crossing — the old placement only cleared the curve at its ENDPOINT, but a falling curve is higher everywhere left of that. Chip now scans the curve across its whole x-span and flips side; badge lifted 2.6u clear with a leader terminating exactly on the ring radius. MEASURED: edge-check 0.0% both aspects, tsc 0, console.log empty, 0 hex, 0 absolute lineWidth, save/restore 11/11, zero THREE references remaining. 'Frozen tail' checked and rejected: p>=0.90 lit-pixel range varies 0.17-0.19%, which is settled, and the mark ring is still pulsing on `idle()`. Residuals: `DRAW_SHARE=0.55` leaves the last 45% of each curve's beat as a breathing hold (deliberate — the narration lands there — but the weakest part of the timing); a label wider than the plot box would overflow symmetrically once `clampRange` collapses to its midpoint; 16:9 uses 62% of content width by design, `MAX_PLOT_ASPECT=2.2` protecting slope readability. |  | 2026-08-06 |
| compare | 19 | Compare | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js slab removed (paint p90 37/32ms -> 1.2/1.0ms, ~30x), single canonical layout (dropped the "divider" 16:9 variant, never distinct from the highlight bar), departT wraps the whole scene for a real exit. motion-check 5/5 both aspects unchanged from pre-rebuild baseline (dead 14%/10%, still 22%/14%, events 6/6); edge-check 0.0%; safe-check clear. | (uncommitted) | 2026-08-08 |
| bracket | 1 | Compare | passed | 1 | 4 | 4 | 4 | 4 | 4 | Reach 9 of 19 kits. Rewritten by a parallel worker, verified here after its run was cut short. Its stated leftover — "replace the hard colour flips with interpolated ramps", the bug this codebase has hand-fixed twice as a "hard colour pop" — **had already landed: `lerpColor` is used 4 times** and no single-frame colour snap remains. Like `chain`, the rewrite is fully 2D: `render3D` and `rotation.` both count **0**. Measured: edge-check **0.0% both aspects**, tsc 0, console.log empty, 0 hex literals, 0 absolute lineWidth, save/restore balanced 4/4. Containment is guaranteed structurally rather than by clamping — `rowPitch = Math.max(unit, (safeBottom - top) / n)` (:218) divides the usable band by the seed count, and the header comment (:197-199) states every chip is placed inside `[contentX, usableRight] x [top, safeBottom]` by construction, so no element can enter the caption band at any p. Verified over p: seeds enter, each round resolves left to right with the winner ramping to amber and carrying a tick, losers dimming to `textDim`, connector lines terminating on the chip borders, and the champion taking a star in the final beat. **Strip-reading caution recorded (third time this session):** the bracket looked like it occupied only the top third with a dead lower half, but the seed rows measure out across 438-1194px of the 1920 frame — a contact sheet cannot be trusted for absolute position, only the layout math can. Residual: unreached seeds render as empty outlined chips with no icon or placeholder text. |  | 2026-08-05 |
| showdown | 1 | Compare | passed | 2 | 5 | 4 | 5 | 5 | 5 | Round-1 diagnosis was RIGHT and its prescribed rework landed: the painter laid the fighters out in pixels, mapped them linearly onto a +/-5.5-unit plane and projected back through a tilted camera at (0,10,7), which does not round-trip against a 9:16 frustum only ~2.23 units wide, so both labels, both score digits and the crown drew off-frame. Camera moved on-axis to (0,0,10) with `mappingAt`/`toWorld` making the pixel rect authoritative; ground plane and grid deleted (an on-axis camera saw them edge-on as lit bands); `makeBlock`'s own wireframe hidden and slabs inset by `1-1/sqrt(2)` so each element carries exactly one outline; every emphasis multiplier capped at <=1 and lean made inward-only so no slab can leave its rect; per-frame slab state routed through `render3D`'s `context` rather than closure capture. **Worst edge 4.3% -> 0.0%, left 4.0% -> 0.0%, right 1.0% -> 0.0%**, both aspects. Per hint 8 this was NOT an asymmetric offset and NOT an unclamped label — the layout was symmetric from centre and BOTH edges were over; the lopsided numbers were an artifact of `"SQL"` being a shorter label that ended further out than `"NoSQL"`. Fixed this round on top of that: the verdict box's `STROKE.thin` was centred ON `safeBottom` so its outer edge stroked into the burned-in caption (short last mark **1322 -> 1320** vs safeBottom 1321.5; long **863 -> 862** vs 863.1), and `cardSlabs[i].radius` -> `slab.radius` for consistency. **Two suspected defects disproved by measurement, not assumed away:** an apparent slab/chrome misregistration (chrome gaps measure symmetric 3/3/3/4px and edge-gradient scans below the cards found zero edges — it was the `GLOW.base` halo), and an apparent frozen tail at p=0.80-1.00 (content-band change measures 4.7-5.9% of pixels per step vs 6.9-8.0% mid-scene, background only 0.28-0.56% — the breathe, glow pulse and crown bob are real). `THEME.danger` deliberately unused: a losing side in a comparison is a verdict, not an error state, so dimming is the correct signal. **edge-check blind spot addressed:** no variants or layout modes exist (the only branch is `layout.vertical`), and the probe's richest fixture `s2-showdown` is the shipping shape. Scores are the worker's measurements; independently re-verified here: 0 hex, 0 absolute lineWidth, no Math.random, save/restore 9/9, tsc 0, edge-check 0.0% both aspects, console.log empty, plus a visual pass at 9:16. Residuals: frame 0 at elapsedMs=0 is blank in both aspects because every element including `drawSceneTitle` starts at `enterT`=0 (the fix is in common.ts); the verdict box's `GLOW.soft` bloom still reaches ~22px below safeBottom at a measured 5/255 luminance, kept rather than cut the 16:9 criterion font 23%; the defeated card reads as a hollow outline (face ~1 luminance step off background at 0.55 opacity); and the 6-round shape the schema allows is reasoned safe but never rendered. |  | 2026-08-05 |
| skyline | 1 | Compare | passed | 1 | 5 | 5 | 4 | 5 | 5 | Reach 5 of 19 kits. Containment and typography were already clean (edge-check 0.0% both aspects, safe-check full clearance) — the real defects were cleanliness and palette. `studioLights()` enables `castShadow` on BOTH its key and rim `DirectionalLight`s, and this painter never disabled the second one: measured a visibly disconnected second shadow patch on the ground plane, a genuine two-light-direction violation of axis 4's single-shadow-direction rule (confirmed by zoomed crops of both aspects). Fixed by disabling `castShadow` on every light past the first. Palette had 4 hardcoded hex plus one absolute `lineWidth = 1.5` (now `unit * 0.05`), and passing `rgba()` strings into `studioLights`/`makeBlock`'s hex params spammed `console.log` with **22 "alpha component will be ignored" warnings** — those materials already set `opacity` explicitly, so the alpha channel was always discarded; passing the plain hex removes the warning with zero visual change. console.log 22 lines -> empty. Residual: per-building 0.08 stagger is code-guaranteed but not independently confirmed at thumbnail resolution; a dead `maxH` layout variable was removed rather than wired into the camera scale — the current fixed per-aspect camera already measures clean at both aspects and rewriting the height mapping had no test coverage beyond eyeballing. |  | 2026-08-07 |
| iso3d | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Unlike the systemic-bug painters in this sweep, already correctly tuned — an off-axis isometric camera with world spread explicitly sized to fill the frame ("Framed tight so the models fill the frame instead of floating small"), plus its own flat 2D fallback for a WebGL failure. Measured 0.0% edge bleed both aspects before this change. 4 hardcoded colour literals -> `THEME` tokens: grid line, chip background, inactive label border, and the shared bright-edge highlight (consolidated onto `THEME.text` rather than yet another near-white one-off — `#eaf3ff` was already duplicated near-verbatim across 11 other still-todo painters). `edge-check` 0.0% both aspects, console.log empty. | 48d8dfe | 2026-08-08 |
| decision | 19 | Wave 2 | passed | 1 | 5 | 5 | 5 | 5 | 5 | Phase 19: already pure 2D, no bug found. Collapsed the per-edge `variantOf(key,2)` curve-side coin flip to one canonical bow direction. departT wired via the shared `introIn` factor; 3 of 6 alpha sites (walk stroke, arrival ping, spark-trail idle effect) had not been multiplying by `introIn` at all and were fixed to do so, so departure and entrance both now cover every element, not just some. Already 5/5 motion both aspects, unchanged; edge-check 0.0%; safe-check clear. | (uncommitted) | 2026-08-08 |
| pipeline | 19 | Wave 2 | passed | 1 | 5 | 5 | 5 | 5 | 5 | Phase 19: already pure 2D, no variant to collapse — only departT was missing. Added it, wrapped in a save/restore across the whole function (a bare `ctx.globalAlpha = leave` without a matching restore would have leaked a dimmed alpha into the engine's overlay draw, which runs right after this call in the same frame — caught before verifying, not after). Already 5/5 motion both aspects: the crawling conveyor dashes, ghost trail and orbiting sparkles already gave it real occupancy. **safe-check reports a -0.5px / 0.0012%-of-band intrusion at 9:16** — a single stray pixel, the exact hairline/antialiasing magnitude this codebase's own convention (`qa/SAFEAREA.md`) already treats as accepted noise, not a defect; recorded rather than chased. edge-check 0.0% both. | (uncommitted) | 2026-08-08 |
| graphwalk | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Pure 2D, no camera, already a centred uniform-scale fit (deliberately aspect-preserving, per its own comment "like statemachine.ts"). Measured 0.0% edge bleed both aspects before this change; the wide 16:9 margins are a square 4-node graph staying square rather than being distorted, not a bug — containment is about clipping/safe-area, not frame-fill %. Real gap was palette: 5 hand-typed hex -> `THEME` tokens / named constants matching `cipher.ts`'s `INK_BRIGHT`/`INK_ON_ACCENT` convention. `edge-check` 0.0% both aspects, console.log empty. | c020433 | 2026-08-08 |
| matrix | 19 | Wave 2 | passed | 1 | 5 | 5 | 5 | 5 | 5 | Rebuilt under Phase 19: three.js layer removed entirely — cell fills, ghost/idle cells and the set-cell pop are now drawn directly in 2D with the exact tone logic the removed material carried (good/warn/dim/accent via a `toneColor()` helper). departT wired into every alpha site. First motion-check failed both aspects (short 2/5 dead24%/still54%, long 2/5 dead20%/still46%): once a cell finished popping in, nothing kept it alive — same "settles then goes fully static" class as memgrid/callstack/tree/table/code/terminal. Fixed by giving the most-recently-set cell a breathing glow (`idle()`-driven shadow blur + alpha) so it stays visibly alive while it's the one the narration is pointing at. 5/5 motion both aspects after the fix; edge-check 0.0% both; safe-check clear both. Verified visually via filmstrip: staggered ghost fade-in, tone-coloured pop-ins, row/col sweep line, clean exit, no clipping. | (uncommitted) | 2026-08-08 |
| queueflow | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Pure 2D, no camera — already correctly built: rotates the whole layout between left-to-right (horizontal) and top-to-bottom (vertical), sized from measured rail/server geometry rather than a literal. Measured 0.0% edge bleed both aspects before this change. 5 hardcoded hex -> `THEME` tokens / a named `INK_FILL` constant matching `cipher.ts`'s convention. `edge-check` 0.0% both aspects, console.log empty. | f12a6e3 | 2026-08-08 |
| calendar | 2 | Diagrams | passed | 2 | 5 | 4 | 4 | 5 | 5 | Reach 7 of 19 kits. Finished a cut-short rewrite whose structure was right but whose stated fixes were half-landed. **The real containment failure is one `edge-check` structurally CANNOT see** — it only samples the outer 3px frame ring, and this content was crossing `safeBottom`, not the frame edge. A pixel scan over 41 p values found the last chip's entrance dip (`translate(0,(1-appear)*unit*0.35)`) painting at **row 1323 against safeBottom 1321.5** at p=0.675 in 9:16, confirmed by backing the fix out and re-measuring, plus an unreserved `shadowBlur` up to `unit*0.65` hanging below it. Both are now reserved out of the layout band (`stackH = usableH - unit*(CHIP_RISE + GLOW_MAX)`): **1323 -> 1281, 40.5px clear, 0 of 41 frames over** (16:9 742, 121.1px clear). **The predecessor's own comment was false and only measurement caught it:** it claimed 'the band thickens out of its ghost outline instead of popping in as a dot', but `Math.max(w, bh)` still rendered a detached filled circle at every band origin — the rubric's literal 'unnecessary dot' — visible in three strip cells. Floor removed; `roundRect`'s radius clamp makes the band wipe out of its own left edge, with `easeInOutCubic -> easeOutCubic` because an ease-in start held it under 10px for ~6 frames. **The band label was drawn in `THEME.bgBottom` from t=0.2 while the sweep only finished at t=0.45, so letters past the wipe front were black-on-black** (rendered as 'Dry summe' at p=0.73); the label now waits for `t - SWEEP_FRAC` and `SWEEP_FRAC` dropped 0.45->0.3 so it is not late. Ghost slots and ghost bands vanished the frame their mark started while the chip began at alpha 0, blinking the element out and back; both now cross-fade under the arrival. **The overflow-label path was reasoned unsafe and then RENDERED to prove it:** a one-month band is only `cellW` wide, so a 16-char label (schema max) takes the above-band branch whose baseline ascends into the month ribbon at lane 0 and onto the previous band otherwise — every lane now reserves a `LANE_GUTTER` strip only that label may occupy. The ribbon-to-lane leader hairline was DELETED rather than shortened: the band is already column-aligned with the months it covers and those cells carry its tone, so it added nothing and crossed the gutter. 16:9 was using 342 of 643 usable px; caps now use 423 with the pair still centred. MEASURED: edge-check 0.0% both aspects, tsc 0, console.log empty, 0 hex, 0 absolute lineWidth, **0 `Math.random` and 0 `new Date(`** (the one kind tempted to read the real clock), save/restore 6/6. Two strip candidates disproved by math: the 9:16 lower void is the mandated caption reserve (content ends 1319 vs 1321.5), and the p=0.80-1.00 'frozen tail' is a settled scene with live glow and shimmer. Residuals: frame 0 at elapsedMs=0 is blank in both aspects because `drawSceneTitle` and every `enterT` start at 0 (fix lives in common.ts); an unstarted mark's ghost slot is an empty outline with no placeholder text (same residual as `bracket`); and two adjacent one-month marks in one lane could in principle collide in the gutter after clamping, which the demo does not exercise. |  | 2026-08-06 |
| globe3d | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Two bugs: marker label chips had no collision awareness — "Subtropical jet" and "Tibetan plateau" project close together and landed directly on top of each other. Fixed with orbit.ts's established resolveChipY greedy-nudge algorithm plus a leader line for nudged chips. Also the 3D viewport started only 30% into the title's reserved band instead of the full band every other painter uses, overlapping the title on 16:9 ("Polar" sat on "winds") — fixed to the full band. Also replaced a re-typed rgba(9,13,18,0.85) literal. console.log clean, 0.0% edge bleed both aspects. | fbf5200 | 2026-08-08 |
| dp_table_fill | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Already solid at both aspects: grid centers/sizes correctly, ghost cells read immediately, dependency arrows trim cleanly to cell edges with no glyph overlap (verified by cropping). Only fix: ghost-cell stroke re-typed THEME.textDim's exact rgb. console.log clean, 0.0% edge bleed both aspects. | d7e0435 | 2026-08-08 |
| sysarch | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | In horizontal layout a replicated tier's stack extends past the primary card's own edge, but an outgoing flow started routing from that edge only — the "query" label between "App servers" (×4) and "Primary DB" rendered as "uery", hidden behind the stack (confirmed visually). Fixed by extending the flow's start point past the stack's actual extent. Also replaced 3 re-typed literals (THEME.bgBottom, THEME.text, INK_ON_ACCENT). console.log clean, 0.0% edge bleed both aspects. | 0539a4e | 2026-08-08 |
| slidingwindow | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Frame slide/resize, pointer chips, index rail, readout panel and L/R/width chips all render cleanly with no collisions across all 4 demo steps (grow, slide, shrink, grow again) at both aspects. Only fix: 4 re-typed literals (3x THEME.textDim, "#08131f" consolidated onto INK_ON_ACCENT). console.log clean, 0.0% edge bleed both aspects. | 76d92c1 | 2026-08-08 |
| trendgraph | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Legend, gridlines, divergence band, series lines/tips, gap chip and x-axis labels all render cleanly with no collisions at both aspects. Only fix: MUTED_HEX was an exact re-typed duplicate of THEME.textDim (removed the local const); also consolidated a near-exact THEME.bgBottom variant. console.log clean, 0.0% edge bleed both aspects. | a6a8abf | 2026-08-08 |
| topology | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The focus caption pill's fixed offset above a device card left only ~0.2 unit of clearance from the kind tag ("HUB") drawn just above it — the pill's shadowBlur bled onto the tag text (confirmed visually). Fixed by increasing the offset; verified the topmost-focus case still clears the title. Also replaced 4 re-typed literals (2x THEME.textDim, THEME.text, INK_ON_ACCENT). console.log clean, 0.0% edge bleed both aspects. | cf70080 | 2026-08-08 |
| scroll | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The wax seal's vertical position was a fixed offset from the sheet's full height, independent of line count, while the text area's floor was fixed only unit*0.5 further down — on a 5-line demo scene the last line's text ran directly under the seal, which rendered on top of it (confirmed visually, "th Amendment" fully obscured). Fixed by reserving the seal's footprint up front when computing the text area's floor, matching the ratioBandH-style reserve-up-front pattern already used elsewhere. console.log clean, 0.0% edge bleed both aspects. | ca1ceac | 2026-08-08 |
| tactical_map | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | A unit near the grid edge (reserve at x=11 of 12) rendered its troop block half outside the map's border — nothing clamped block position to the map bounds, and isoBox3D's depth extrusion extends the visible edge further than the block's own size (confirmed visually and via debug logging). Fixed by clamping each block's centre to stay inside the panel including the extrusion. Also replaced 3 re-typed literals (INK_ON_ACCENT, THEME.text, THEME.bgBottom). console.log clean, 0.0% edge bleed both aspects. | 2fb7a75 | 2026-08-08 |
| architecture_blueprint | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Pure 2D, no camera — already correctly built with the `diagram.ts`-style centred, capped grid-fit (`gridMap`, f<=1.25). Measured 0.0% edge bleed both aspects before this change. 2 hardcoded hex -> named constants matching `cipher.ts`'s `INK_BRIGHT` convention / `THEME.bgBottom`. `edge-check` 0.0% both aspects, console.log empty. | 9a8e2d9 | 2026-08-08 |
| packet_delivery | 2 | Wave 2 | passed | 1 | 4 | 4 | 3 | 4 | 4 | Layout/motion already solid overall. Found a minor pre-existing overlap (payload pill grazes the arrived-at hop's icon by ~unit*1.3) and attempted a clearance fix, but it just pushed the caption above into the departure hop instead — confirmed worse across multiple steps, reverted. This vertical-stack squeeze needs a position-adaptive fix, not a fixed offset; left as a lower-severity residual gap (typography 3/5, disclosed rather than hidden). Fixed 3 literals: "#eaf3ff" (THEME.text), 2 near-blacks consolidated onto THEME.bgBottom/THEME.panel. console.log clean, 0.0% edge bleed both aspects. | d7b0f6f | 2026-08-08 |
| codediff | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | On 16:9, fontPx/lineH were sized from line-width fit only, then panel height fh was separately Math.min-clamped to the smaller available vertical space — line Y-positions stayed based on the unclamped lineH, so the last diff lines rendered past the clip rect and the status bar sat on top of the truncated text. Now derives a height-driven line-height cap from availH up front and takes the min against the width-driven size, so fh is always consistent with the lineH used to place lines. Also swapped hardcoded editor-chrome hex (title-bar gradient, gutter strip, pending-line dashes, status bar bg) for THEME-derived colors, matching code.ts; kept the traffic-light dots as the precedented literal exception. console.log clean, 0.0% edge bleed both aspects. | c37d4ec | 2026-08-08 |
| parliament_arc | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Already solid: majority-line math correct (two-thirds of 545 = 363), flag-label clip-avoidance fallback works, legend's contiguous dot+label+count grouping already well-designed. Fixed 3 literals: 2x rgba(148,163,184,X) (THEME.textDim), 1x "#0a0e13" (THEME.bgBottom). console.log clean, 0.0% edge bleed both aspects. | ebc617f | 2026-08-08 |
| server_rack | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The group-isolation boundary is one shared bounding box while each rack's label is projected independently — camera perspective meant the two didn't hold a fixed relationship across rack X positions, so "Rack B"'s label was bisected by the dashed boundary line while "Rack A"'s sat clear (confirmed visually). Fixed by clamping member racks' label y below the group's actual computed boundary bottom. Also replaced 3 re-typed literals (THEME.text, THEME.bgBottom, THEME.textDim). console.log clean, 0.0% edge bleed both aspects. | bf126cc | 2026-08-08 |
| jigsaw_puzzle | 2 | Wave 2 | passed | 3 | 4 | 4 | 4 | 4 | 4 | Three related bugs in the piece-label path: the "not-fit" X mark is centred on the seam with its own radius, always spilling into the neighbour's label text ("authentication" crossed) — fixed by nudging it fully inside the mismatched piece. Icon+2-line-label+sub-label sized only against width could outgrow the piece's bottom edge — two reposition attempts just relocated the overflow; fixed properly by shrinking both text sizes together against the actual vertical budget. wrapText(...).slice(0,2) silently dropped "login" from a 4-word label on a narrow 16:9 piece — raised the cap to 3 lines. Also replaced 1 re-typed THEME.textDim literal. console.log clean, 0.0% edge bleed both aspects. | 1eba7ca | 2026-08-08 |
| domino_cascade | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Emoji icons (worker/factory) rendered as a dark smudge against the domino's own saturated purple face — confirmed by cropping and comparing against coin_stack.ts's known-good emoji render (glyphs are correct, just a demo-emoji-vs-accent contrast gap, not a broken glyph). Fixed generically with a small light neutral plate behind every icon so any emoji stays legible on any tile colour, rather than special-casing specific emoji. Also swapped "#eaf3ff" for THEME.text. console.log clean, 0.0% edge bleed both aspects. | 598c87c | 2026-08-08 |
| sheet_music | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Staff lines, notes, slide ties, ledger stubs, note labels, playhead sweep and the tala cycle strip all render cleanly with no collisions across all three demo rows (mixed sitar/sarod voices) at both aspects. Only fix: 3 re-typed THEME.textDim literals. console.log clean, 0.0% edge bleed both aspects. | ab8d841 | 2026-08-08 |
| canvas_reveal | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Swatch labels overlapped into unreadable text — `fitFontSize`'s `maxW` for a label was `chipW*1.7`, 70% wider than the chip itself, while chips are packed with only a `unit*0.6` gap. Measured concretely: "Tandul white" ran straight into "Rice-husk black" on the first render. `maxW` is now the chip's own slot (width + a little of its trailing gap). Also: caption background was a hand-typed `rgba(9,13,18,0.72)` — an exact match for `THEME.bgBottom` — swapped in. `edge-check` 0.0% both aspects, console.log empty. | addb7c4 | 2026-08-08 |
| scalecompare | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The reserved floor height for the closing verdict caption (a flat unit*2.4) and the verdict text's own baseline offset (independently hardcoded) didn't actually agree, so the tall bar silhouettes rendered straight through the verdict pill's text — confirmed via cropped screenshot. Fixed by deriving both the reserved space and the verdict's position from the actual wrapped-line measurements, anchored off the same boundary that constrains the bars. Also replaced 2 re-typed literals (THEME.bgBottom, THEME.textDim). console.log clean, 0.0% edge bleed both aspects. | af7d63b | 2026-08-08 |
| fluidflow | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Two containment bugs on 9:16 (edge-check 0.8%: 0.5% left, 0.8% right). Particle streamlines faded over a margin computed from the map's own edge, but the map fills contentW with zero horizontal slack — fixed by clipping particle rendering to the content box. "Bay of Bengal"'s label chip always grew rightward with a flat max width and ran off the frame near the map's right edge — capping maxW still overflowed at that position, so fixed properly by flipping the chip leftward when that side has more room (mirrors event_loop.ts's edge-aware placement). Also replaced a re-typed rgba(9,13,18,0.78) literal. console.log clean, 0.0% edge bleed both aspects (was 0.8%). | bc0054e | 2026-08-08 |
| ecosystem_web | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Layout/motion/containment already solid at both aspects. The 3-node triangle only using the top ~60% of its ellipse is inherent to 3 nodes at 120° spacing (one hits the top extreme, the other two sit at ±30° elevation, not the bottom extreme) — verified against layoutNodes()'s geometry, not a bug; 4+ node webs use the full ellipse evenly. Fixed 4 literals: three rgba(148,163,184,X) re-typed THEME.textDim; one rgba(10,16,22,0.82) re-typed THEME.bgBottom (same unfixed variant still in server_rack.ts/sysarch.ts). console.log clean, 0.0% edge bleed both aspects. | 46b6510 | 2026-08-08 |
| turing_tape | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The state chip's offset put its own bottom edge inside the head triangle's span, hiding nearly half of the head pointer behind the chip whenever a state label was present (confirmed via geometry and visually). Fixed by moving the chip up to clear both the triangle and the glide-arrow cue. Also replaced 2 re-typed literals (THEME.textDim, INK_ON_ACCENT). console.log clean, 0.0% edge bleed both aspects. | ad8333a | 2026-08-08 |
| grid_flood | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Already solid: BFS ripples, confluence gold highlight, and seed glow rings all correct (the faint circles around seed cells are the documented always-on seed markers, not an artifact). Fixed 2 rgba(148,163,184,X) literals re-typing THEME.textDim. console.log clean, 0.0% edge bleed both aspects. | ff1b3a0 | 2026-08-08 |
| hash_ring | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | "Server C" ran off the left edge on 9:16 (edge-check 0.8% left) — the label already grows away from its icon based on ring side, but maxW was still a flat unit*4.2 regardless of actual room to that edge. Fixed by capping maxW to the room available in the label's growth direction. Also replaced "#eaf3ff" (THEME.text) and "#08131f" (consolidated onto the shared "#06121a" INK_ON_ACCENT). console.log clean, 0.0% edge bleed both aspects (was 0.8%). | b71f58b | 2026-08-08 |
| recursion_tree | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Tree connectors, prune X marks, accept checkmarks and the call-stack panel all render cleanly with no collisions in both single- and multi-branch demo frames at both aspects. Only fix: 2 re-typed THEME.textDim literals. console.log clean, 0.0% edge bleed both aspects. | 668bba3 | 2026-08-08 |
| token_exchange | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The note caption's fixed vertical offset from its card crowded the actor chips for the first row in dense scenes — "exp: 15m" rendered touching the "Client" chip with no visible gap (confirmed visually). Fixed by flooring the caption's y to a fixed gap below the lifelines' start. Also: DENY was an exact re-typed duplicate of THEME.danger (added after this file's own stale comment claiming no error color existed) — replaced with THEME.danger; 5 more re-typed literals consolidated (THEME.text, THEME.textDim x2, INK_ON_ACCENT, INK_FILL, THEME.bgBottom). console.log clean, 0.0% edge bleed both aspects. | 9f27335 | 2026-08-08 |
| coin_stack | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Layout/motion/containment were already solid (0.0% edge bleed both aspects, smooth staggered entrance, coins arc correctly between stacks, locale-correct ₹ grouping). Fixed 3 palette literals: DANGER re-typed THEME.danger's exact hex (the constant exists because 12 painters did this before it existed), the baseline strip re-typed THEME.textDim as raw rgb, and the flight-label chip bg was an untethered near-black instead of THEME.bgBottom. console.log clean. | 44f4c7f | 2026-08-08 |
| btree_index | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Pure 2D, no camera — already well composed on both aspects. Measured 0.0% edge bleed both aspects before this change. 3 hand-typed textDim rgba triplets -> `rgba(THEME.textDim, ...)`. `edge-check` 0.0% both aspects, console.log empty. | b64837b | 2026-08-08 |
| lsm_compaction | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Already solid at both aspects: memtable fill/flush, compaction merge lines, and tombstone-drop marks all read correctly (a suspicious faint yellow blob near a fading source card turned out to be that card's own tombstone-count badge at reduced alpha, verified by walking the full filmstrip sequence). Fixed 2 literals: rgba(148,163,184,0.5) re-typed THEME.textDim; "#1a1204" consolidated onto the shared "#06121a" INK_ON_ACCENT. console.log clean, 0.0% edge bleed both aspects. | 8f56eeb | 2026-08-08 |
| vdom_diff | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The prop-drill token lands exactly at the destination node's centre — the same spot its own label renders — and stayed fully opaque through the last stretch of travel, so "props" rendered on top of "Item: Milk" (confirmed visually). Fixed by fading the token out as it completes its journey; the arrival glow ring still marks the destination. Also: DANGER was an exact re-typed duplicate of THEME.danger; 3x "#0b0f14" consolidated onto INK_ON_ACCENT, 1 THEME.textDim. console.log clean, 0.0% edge bleed both aspects. | fc9d6ab | 2026-08-08 |
| flamegraph | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Duration text ("180ms" etc) inside wide bars used THEME.textDim against the bar's own bright accent/warn fill — too little contrast, same class as container_sandbox.ts's caption bug. Fixed by dimming the same white the bar label uses successfully. Also replaced a re-typed rgba(148,163,184,0.14) gridline literal. console.log clean, 0.0% edge bleed both aspects. | c5201dd | 2026-08-08 |
| event_loop | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | "fetch_orders()" ran off the right edge on 9:16 (edge-check 0.3% right) — a waiting-arc task's label grows away from its chip with a flat unit*5 max width and no edge check. Fixed by capping maxW to the actual room left toward whichever edge the label grows into. Also named 5 one-off literals (2 matching cross-file conventions: INK_FILL, SPARK; 3 unique to this file) and replaced 2 rgba(148,163,184,X) re-typing THEME.textDim. console.log clean, edge-check 0.0% both aspects (was 0.3%). | db5dea3 | 2026-08-08 |
| js_event_loop | 1 | Code | passed | 3 | 4 | 4 | 4 | 4 | 5 | New kind, 4/4/4/4/5 in 3 rounds. **The JS event loop as a machine**: call stack, web-APIs lane, microtask queue and macrotask queue, and the rule that connects them — the stack empties, then EVERY microtask runs, then exactly ONE macrotask is picked up. A `takeMacro` issued while microtasks remain is **refused on screen** with a warn badge, so the painter enforces the ordering rather than trusting the script; the demo authors that refusal at step 9. `drainMicro` empties the whole queue one chip at a time inside a single beat, so "all of them, not one" is watched rather than narrated. Deliberately **2D with no camera**, so none of the `2d-layout-round-tripped-through-camera` modes its 14 predecessors were fixed for can apply. R2: region heights come from their content (a proportional split left each lane about twice the height of the chips in it) and the scale factor is solved exactly — `fixed + scalable * fit = availH` — rather than approximated, which had let the group overrun `safeBottom` by a few px because headers and gaps counted toward the estimate but do not grow; the phase caption also moved inside its lane from the gap beneath it. R3, from the `--entrance` read: all four regions faded in on one tick, which the motion axis calls simultaneous — they now cascade via **`stagger`**, the row-9.0 helper no painter had used yet, so stack/APIs/micro/macro arrive at 0/70/140/210 ms and the entrance teaches the reading order. Slot counts computed over the whole scene so no lane resizes between beats; every departure drawn leaving. 0 colour literals, 0 absolute-px strokes, console.log empty, `edge-check` 0.0% both aspects. Fixtures: `jsloop` (the canonical setTimeout-vs-Promise lesson, 13 steps) and `jsloop-starve` (a microtask that queues a microtask). | 46b784d | 2026-08-01 |
| dom_event_flow | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Design/containment already solid — the single fixed-angle ray from the shared box center crossing every nested box border exactly once is a deliberate, correctly-implemented motif (verified against the geometry code and both aspects), not a bug despite looking unusual at first glance. Fixed 5 literals: rgba(148,163,184,0.9) re-typed THEME.textDim; "#08131f" was a one-off ink variant where 8 other files share "#06121a" as INK_ON_ACCENT; three rgba(9,13,18,X) re-typed THEME.bgBottom. console.log clean, 0.0% edge bleed both aspects. | 2a732e0 | 2026-08-08 |
| commit_dag | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | On 9:16, same-lane consecutive commits share an x, so the straight parent->child edge line ran directly through the commit's own id label (fixed offset below the dot, same vertical line) — confirmed by pixel-cropping the rendered frame. Gave each label a small halo (roundRect, THEME.bgBottom) sized to its measured text box, drawn before the text; 16:9 was unaffected since labels sit off the horizontal edge axis there. Also swapped 6 raw rgba(148,163,184,X) literals for rgba(THEME.textDim, X). console.log clean, 0.0% edge bleed both aspects. | 89ece7d | 2026-08-08 |
| partitioned_log | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Consumer flag chips ("worker-1 · lag 0" / "worker-2 · lag 1") could overlap when two consumers landed at nearby offsets in the same lane — the old dedupeX helper only avoided exact-pixel anchor collisions, never accounting for each chip's actual rendered text width. Confirmed visually (first chip's text hidden behind the second). Fixed with a width-aware resolveFlagX/flagChipHalfWidth greedy collision resolver. Also replaced 4 re-typed literals (3x THEME.textDim, 1x INK_FILL). console.log clean, 0.0% edge bleed both aspects. | 4b0e731 | 2026-08-08 |
| container_sandbox | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The "sees N/M" caption inside the process box used THEME.textDim against the box's bright accent face — too little contrast to read, confirmed by cropping the rendered frame. Fixed by dimming the same white the process label above it already uses successfully, instead of a fixed muddy grey. Also replaced "#eaf3ff" (re-typed THEME.text, the same literal iso3d.ts's polish consolidated) on both server icons, and one rgba(148,163,184,0.4) re-typed THEME.textDim. console.log clean, 0.0% edge bleed both aspects. | b1d1e00 | 2026-08-08 |
| control_loop | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Layout/containment/motion already solid (0.0% edge bleed both aspects; the thin dark sliver on each card's right edge is the standard isoBox3D depth-face convention, not a bug — confirmed by cropping). Fixed 6 literals: DRIFT plus a second inline re-type of the same value both duplicated THEME.danger; card ink used a one-off "#0b1016" where 8 other files already share "#06121a" as INK_ON_ACCENT — consolidated; gear icon re-typed THEME.text. console.log clean. | a041f76 | 2026-08-08 |
| telemetry_trace | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Tree-rail bracket guides, time axis, span bars (sequential + parallel overlap), duration labels and the verdict banner all render cleanly with no collisions across a 7-span nested trace at both aspects. Only fix: 3 re-typed literals (THEME.textDim, 2x THEME.bgBottom near-miss variants). console.log clean, 0.0% edge bleed both aspects. | ab0bfc8 | 2026-08-08 |
| spatial_index | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Points near a capacity-triggered split land within a pixel of each other by design, but each point's label was independently centred above it at a fixed offset — "D1" and "D2" rendered on top of each other, unreadable (confirmed visually). Fixed with a resolveLabelY collision resolver, same pattern as orbit.ts's resolveChipY. console.log clean, 0.0% edge bleed both aspects. | bf09250 | 2026-08-08 |
| object_heap | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The step-note caption was positioned at contentY+contentH (== layout.safeBottom, the documented caption-safe boundary) plus an extra offset, landing at/past the exact line it's meant to stay clear of — the vertical clamp fell even deeper into the burned-in caption band. Fixed to draw comfortably above safeBottom. Also replaced 5 re-typed literals (2x THEME.textDim, 2x THEME.text, 1x consolidated onto shared INK_ON_ACCENT). console.log clean, 0.0% edge bleed both aspects. | 500996f | 2026-08-08 |
| vector_space | 2 | Wave 2 | passed | 2 | 4 | 4 | 4 | 4 | 4 | A distance-label chip centred on its point pair's raw midpoint ran past the plot's left edge into the rotated y-axis label for a pair near it ("man"/"woman"). First fix (clamp both edges) over-corrected, colliding the right-side chip into the left one instead — only the left edge carries a real hazard, so only that side is clamped. Also replaced 5 re-typed literals (3x THEME.textDim, 2x THEME.bgBottom). console.log clean, 0.0% edge bleed both aspects. | e49a0fc | 2026-08-08 |
| neural_network | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | "∂L/∂W1" rendered with a mangled, upside-down-looking glyph for ∂ — reproducible across all 3 occurrences, confirmed by cropping the frame. Since demo fixtures render through the same engine as real videos, fixed in demo.ts by swapping ∂ for the ASCII-safe "d" (dL/dW1). Also replaced 3 re-typed literals in the painter (2x THEME.textDim, 1x THEME.bgBottom). console.log clean, 0.0% edge bleed both aspects. | a3b4598 | 2026-08-08 |
| matrix_convolution | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | The elementwise-product overlay's dark backing was sized only to its own text, drawn over the input cell's own number at the same centre — for a weight-1 kernel (product==input value) the input's own glyph peeked out from behind, reading as a smeared double-image. Confirmed visually, fixed by sizing the backing to the cell itself. Also replaced 3 re-typed literals (2x THEME.textDim, 1x THEME.bgBottom). console.log clean, 0.0% edge bleed both aspects. | 0ff1d3e | 2026-08-08 |
| consensus_quorum | 2 | Wave 2 | passed | 1 | 4 | 4 | 4 | 4 | 4 | Layout/motion/containment already solid: ring geometry keeps adjacent node circles well separated at both aspects (verified nodeR stays capped below the sin-based touching threshold), quorum meter/threshold marker/arrows all read correctly. The overlapping glow around adjacent commit-toned nodes in one still frame is glowRing's per-node pulse phase coincidentally aligning — decorative, transient, same shared helper already accepted elsewhere. Only fix: DANGER re-typed THEME.danger's exact hex. console.log clean, 0.0% edge bleed both aspects. | 34e3d51 | 2026-08-08 |
